import { describe, expect, it } from 'vitest'

// Phase 0 only proves the toolchain runs. Real tests start in Phase 1.
describe('scaffold', () => {
  it('runs the test framework', () => {
    expect(true).toBe(true)
  })
})
