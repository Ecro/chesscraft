---
generated_by: harness-maker
harness_maker_version: 0.55.0
generated_at: '2026-01-01T00:00:00+00:00'
source_template: memory/failures.ko.md.j2
provenance: official
---
# Failures Log — Side preset

> 이 프로젝트에서 반복된 실수 / 함정을 기록합니다. wrapup 스테이지가 자동 추가합니다.
>
> **검색:** `rg -F "[fail:" .claude/memory/failures.md`
>
> **형식:**
> ```
> ## [fail:<category>] <slug> | <YYYY-MM-DD> | count:<N>
> <재현 조건 + 원인 + 해결책 한 단락>
> - [<YYYY-MM-DD>] <2번째 발생 한 줄 노트>
> - [<YYYY-MM-DD>] <3번째 발생 한 줄 노트>
> ```
> - `category`: import / test / render / hook / lint / type / runtime / design / other
> - `count`: 동일 실수 반복 시 헤딩만 업데이트 — 같은 slug → count++ (중복 섹션 금지). wrapup 이 기존 slug 를 먼저 검색해 일치하는 것을 재사용하므로 재발이 분산되지 않고 누적됨.
> - 반복마다 원본 본문 아래에 날짜별 발생 bullet 한 줄이 추가됨 (헤딩의 first-seen date 는 보존) — 따라서 `count:N` 엔트리는 N−1 개의 발생 근거 줄을 담음.
> - count ≥ 3 이면 wrapup 이 `.claude/memory/pending-proposals.md` 에 개선 제안 추가

---

<!-- @hm:user:entries -->
(아직 기록된 실패 없음)
## [fail:design] empty-collection-is-not-absent | 2026-08-05 | count:1
A feature that ACTIVATES on a filtered collection silently produces an EMPTY activation instead of no activation, and the empty form is truthy. Concretely: the second skill draft filters the card pool by "not already offered, not already held"; with a pool of exactly one offer's worth the filter empties, `pickDistinct` returned `[]`, and `offers = []` still gated board play while yielding zero `draft_pick` actions — a match with no result and no legal action, reachable from perfectly valid content. Fix was two-part and both parts matter: the producer refuses to open an offer it cannot fill (`drawn.length === DRAFT_OFFER_SIZE`), and the consumer treats an empty collection as absent (`(offers?.length ?? 0) > 0`, not `offers ?`). The guard that actually catches this class is a loop-invariant assertion over randomized play — "if the match has no result, it has at least one legal action" — not a scripted line, which never reaches the boundary. Generalization: whenever a filter feeds a fixed-size requirement, write down what happens when the filter empties, and assert the invariant rather than the happy path.
## [fail:design] shared-vocabulary-unshared-code-path | 2026-08-05 | count:5
One shared vocabulary, two code paths — and only one of them ran the pipeline. `apply` routed a board move through the E1..E7 lifecycle but sent a card play straight to E6/E7, so the SAME declarative action (`teleport_piece`) fired the destination square's `on_enter` when a square or piece owned it and silently skipped it when a card owned it. A warp became the only way to walk onto a hostile square unharmed, and nothing in the schema showed that — the two actions are literally the same word. The cost is not the bug, it is that no content author could ever have found it: the vocabulary promised sameness the interpreter did not keep. Fix: extract the cascade (`cascadeEnter`) and route both branches plus E5 promotion through it. The regression test that pins it asserts a board move and a card-driven move produce the SAME resolution log. Generalization: when a design claims "X means the same thing regardless of who owns it", write the test that drives X from each owner and compares, because a shared grammar over unshared code paths is the failure that hides best. `spawn_piece` still has this shape and is recorded as an open gap rather than quietly fixed.
- [2026-08-05] Second instance, same root cause: spawn_piece (and the new revive_piece) put a piece on a square without running E4 entry, so creation was the remaining safe way onto a hostile square. Closed in Phase 6a by draining an arrived-squares list through the same cascade.
- [2026-08-09] Third instance, and this time the two paths were a rule I had just written. The copy-on-edit rule for shipped content lives in `src/editor/fork.ts` and is called from two save paths — `RecordForm.prepared()` and `RoomDetail.save()` — and only the first ordered it correctly. `RecordForm` forks and THEN folds the typed name onto the copy's key; `RoomDetail` folded the name first, onto the ORIGINAL's key, so the fork then resolved that already-overwritten key and copied the child's new name onto the copy as well. Renaming a shipped room while also changing its rules therefore renamed the shipped room too, and both rooms rendered the same name. Extracting the rule into one shared module was not enough, because what differed was not the rule but the ORDER the caller invoked it in — a shared function still has two call sites, and the sequencing around it is per-site. Found by a cross-model reviewer, not by the suite: the room-fork test changes composition and never renames, the rename test changes no rule, and the combination is one ordinary save for a child. Guard: when one rule has N call sites, write the test matrix over the CROSS PRODUCT of the actions each site can perform in a single operation, not one test per action — the defect lives in the combination, which is exactly what per-action tests cannot reach.
- [2026-08-09] Third instance, in the EDITOR rather than the engine. The room maker and the board record maker write the identical model (boardDef.placements and boardDef.squares) through two hand-written reducers, and the two had drifted: a tap on an occupied square cleared it in the room whatever piece was held but only on an exact piece-and-side match in the board record, and painting over one half of a portal pair replaced that entry in place, leaving the other half pointing at a square that no longer paired back — a document the loader refuses, produced by an ordinary tap. Extracting the markup into a shared component fixed the LOOK and left both reducers duplicated; a cross-model review found the placement drift and a second round found the paint drift still there. Fix: togglePlacement and paintSquare as shared pure reducers in PlacementPainter.tsx, with the room semantics winning because they were the ones with a written argument. The differential test that pins it drives the same tap sequence through both surfaces and compares, which is this entry own generalization applied one layer up.
- [2026-08-14] 2026-08-14 v12: editor/io.ts held a stale copy of the loader normalization (pre-v12 imports refused) and grading-agent.ts a stale copy of the action budget (matches silently truncated); both deleted in favour of one exported definition. A third copy of the budget still lives in tests/engine/invariants.test.ts.
## [fail:tooling] spec-machine-binding-is-pytest-only | 2026-08-05 | count:6
`hm spec_machine mark-tested` validates every `--test-id` through `pytest --collect-only`, so in a TypeScript project (vitest + Playwright) NO acceptance criterion can ever be forward-bound: it prints `rule-3: test_id does not resolve via pytest --collect-only` and leaves the machine SPEC untouched. The trap is not the rejection, it is what the untouched file then says — all 18 ACs sit at `pending_test: true`, which reads as "nothing is tested" when in fact 10 of them have passing vitest/Playwright tests. Anything downstream that trusts `pending_test` (coverage reports, an unbound-AC audit, a future gate) will draw the wrong conclusion in the safe-looking direction. Do NOT hand-edit `pending_test` to compensate — that trades a wrong-but-visible state for a wrong-and-invisible one. Until the tool grows a non-pytest collector, the AC-to-test mapping for this repo lives in the PLAN's acceptance checklist, and any wrapup must say out loud that the machine SPEC's binding state is structurally stale rather than reporting "0 bound" as a finding.
- [2026-08-06] Same Python assumption, different entrypoint: `hm second_opinion_oracle` runs only `uv run pytest` and `uv run ruff`, so on this TypeScript/React repo it emitted a pytest "not found" for the .tsx path plus ~1.2 MB of ruff `invalid-syntax` from parsing TSX as Python, and produced no usable oracle for any cross-model finding. The PIDA gate treats a missing oracle as absence rather than refutation, so unnoticed this degrades every second-opinion finding toward `unresolved` while looking exactly like a working run. Worked around by substituting fixed-command project evidence and saying so in the REVIEW.
- [2026-08-08] Third instance, review stage: hm second_opinion_oracle ran uv run pytest + ruff against .tsx and produced 284KB of invalid-syntax and no usable oracle, so every cross-model finding would have degraded toward unresolved; substituted a direct vitest reproduction as project-native evidence and said so in the REVIEW.
- [2026-08-09] Fourth instance, wrapup Step 3.5: mark-tested rejected every one of 13 acceptance criteria with rule-3 test_id does not resolve via pytest --collect-only, so the machine SPEC still reads pending_test: true for all 13 while every one has a passing vitest or Playwright test named in the SPEC verification table. Reported as structurally stale rather than as zero bound, per this entry own instruction.
- [2026-08-14] 2026-08-14: confirmed again by attempt rather than assumption — mark-tested rejected a vitest node id with "does not resolve via pytest --collect-only", so all 12 ACs of a TypeScript unit stay pending_test:true with their coverage recorded only in the SPEC prose table.
- [2026-08-26] Fourth instance, wrapup Step 3.5: mark-tested rejected a vitest node id with rule-3 test_id does not resolve via pytest --collect-only, leaving all 7 pytest-bindable ACs at pending_test: true although all 7 have passing vitest/Playwright tests. Reported out loud as structurally stale rather than as 0 bound, and pending_test was not hand-edited.
## [fail:design] declared-but-inert-vocabulary | 2026-08-05 | count:8
A schema entry that validates but that no interpreter ever reads is worse than a missing one: the author gets a card that passes every check, draws, plays, and does nothing, with no signal anywhere. Two instances shipped this way — `check_count_at_least` (a condition hardcoded to return false because check detection did not exist) and `destination: own_back_rank` (declared in the grammar, never handled in the action switch). Both were caught only by trying to author a card that used them. A third is still open: `grant_movement`, `forbid_movement` and `block_capture` are consumed at move-generation time only, so a skill card carrying any of them is a silent no-op while the skill-card schema happily accepts all nine actions. The rule that would have prevented all three: every vocabulary entry needs either a test that drives it end to end from real content, or an explicit rejection at validation time saying it is not available in this position — returning a safe-looking default (false, null, no-op) is what makes the failure invisible. When adding an entry ahead of its implementation, make the validator refuse it until the implementation lands.
- [2026-08-06] Same shape in CSS: --color-focus and --color-board-dark were declared in all three cascade layers of a new token file and referenced by no selector. --color-board-dark is the costly one — it implied a checkered board that does not exist, so the flat single-colour board read as intentional to everyone including the author of the audit that missed it.
- [2026-08-06] Third instance, and this one was created BY the fix for the thing it is about. A review finding said deletion dropped overlay text by id prefix, which is wrong once a record keys its text outside its own namespace; the repair replaced that call with a reachability walk and left dropStrings exported with no caller but its own unit test — the exact shape of this entry, re-created by the repair, while the PLAN note written minutes earlier claimed 9b had CLOSED an instance of it by giving dropStrings a caller. Found by re-reading my own work, not by a reviewer. dropStrings and its two tests were deleted and the PLAN corrected. Generalization: when a fix REPLACES a helper rather than changing it, the helper it replaced is now a candidate instance of this entry, and a passing unit test is what will make it look alive.
- [2026-08-07] Fourth instance, found by playing rather than by reading: the rule card 피의 대가 (rule.blood-toll, "the piece that captured disappears too") fires its effect and changes nothing. Verified by driving the engine — the log records on_capture:rule:rule.blood-toll and the capturing pawn is still standing on the destination afterwards. Cause is timing, not a missing handler, which is why a static scan cannot see it: engine.ts removes the occupant BEFORE runEvent and places the capturer AFTER it, so at on_capture the subject square is EMPTY and both `entering` and `mover` resolve to it. There is no target kind that names the capturing piece at that moment, so the card is unauthorable as written. UNFIXED as of 2026-08-07 and deliberately left for its own unit: the repair is an engine semantics change (make on_capture bind the capturer at action.from) under ADR-002 event ordering, and the blast radius is exactly one record because blood-toll is the only content anywhere that uses on_capture. Whether other cards are inert for their own timing reasons is UNKNOWN — answering it needs an empirical per-card survey, not a grep.
- [2026-08-07] Fifth instance, and the empirical survey this entry asked for is now done: all 26 shipped cards driven from hand-built positions, each compared against a no-card control on both surfaces a card can act through (resolved state, and the legal move set). Result: 22 of 26 live, 4 broken, from 3 root causes. blood-toll is the on_capture ordering above. The other two causes are ONE vocabulary entry read in one of the three places it is needed -- forEach. (a) bindEffect sets boundSubject, and only generationModifiers reads it; runEvent builds its EvalCtx from the ply-global subject, so a forEach effect on a lifecycle trigger evaluates its condition against the piece that MOVED while its action targets the piece the quantifier bound. king-of-the-hill therefore wins the match for a rook on the centre with the king still at home, and fast-promotion promotes a pawn on c3 because a rook landed on e2. (b) The card-play branch of apply builds its BoundEffect by hand and never calls bindEffect at all, so forEach is dropped entirely for skill cards: skill.charge offers one play with no targets and grants nothing. Two lessons the survey itself taught. First, liveness is not the property to test -- 3 of the 4 defects are cards that DO change state, just the wrong state, so a probe asking only did-anything-happen would have cleared two of them; the sharp probes are the ones asserting a card stays INERT where it must (a rook in the centre must not win). Second, one of those inert-expected probes was confounded on first writing -- it parked the pawn on rank 5, where promotion was legitimate under both the buggy and the correct engine -- and only moving the pawn to c3 made it discriminate; a probe for wrong-firing must place the subject where the CORRECT behaviour is provably nothing. Diagnosis validated by mutation: adding boundSubject to the runEvent context flips exactly those 3 probes and leaves the other 27 green. Recorded as tests/engine/card-liveness.test.ts, which asserts current behaviour rather than intended so it is green on the broken engine on purpose and goes red the moment any card changes in either direction. CORRECTION to this entry: the third open instance above -- grant_movement / forbid_movement / block_capture being a silent no-op on skill cards -- is CLOSED, and was closed by the schema v3 duration parameter. knight-leap, bulwark and shackle all measured live. charge is inert for the unrelated forEach reason, which is exactly how a stale entry keeps looking confirmed.
- [2026-08-08] Sixth instance, and the first found by a cross-model reviewer rather than by the survey: square.geyser logs on_enter and leaves the piece standing, because own_back_rank resolves through a vacancy lookup that returns null on a full rank — and every room STARTS with a full home rank, so the square is dead for exactly the opening in which a piece first steps on it. The card-only survey could not see it: SPEC AC-013 says "every new CARD", and square types had no liveness rig at all. tests/engine/square-liveness.test.ts now drives all 8, pins both geyser branches, and fails when a square type is added without a probe. Also, this entry itself went stale and misled three stages — see stale-memory-taken-as-current.
- [2026-08-08] src/balance/cost.ts has stated in its own header since ADR-012 that the price model is explainable — "this one costs more because it reaches further" — and no screen ever rendered a word of it. The badge showed a star count and a generic caveat, so an author could only ask why a piece got the stars it got, which is exactly what they did ask. The property was designed in, documented, and never wired out. Fixed by returning the TERMS of the same expressions the price evaluates rather than writing the weights into help text, which would be a second description that goes stale the day a weight moves.
- [2026-08-14] 2026-08-14 v12: the settlement step written to close this failure re-opened it — an end_of_ply freeze on `mover` queued into an already-drained list, no freeze and no settle:dropped, while the log still claimed it fired. And the replacement capture card shipped at plies:2, which expires as the capturer own next turn begins, so the tax charged nothing.
## [fail:test] all-positive-fixture-hides-overcounting | 2026-08-05 | count:4
A fixture in which every instance is a positive cannot detect an implementation that ignores the predicate entirely. Concretely: a three-check win condition was tested with a move sequence where every one of the mover's moves genuinely delivered check, so an engine that incremented the counter on "the mover completed a ply" — never evaluating whether check was actually given — produced the identical counter sequence and passed the whole describe block including the win-on-third assertion. The reviewer caught it before implementation; had it landed, the bug would have shipped behind a green suite. Fix: insert a deliberate DUD into the sequence (a mover move that does not satisfy the predicate) and assert the counter is unchanged across it. Generalization: for any counter, filter, or guard, the load-bearing fixture is the negative instance interleaved with the positives — not more positives, and not a separate happy-path test. The same reasoning produced the paired control elsewhere in that file: assert the protected case AND the identical position with the protection removed, so the assertion is about the mechanism rather than about the position.
- [2026-08-05] Same root cause, different disguise: a duration-expiry fixture moved the granted piece to another square before asserting the grant was gone, so a square-keyed store orphaned it and the test passed with no expiry logic implemented at all. The reviewer predicted the storage shape from the frozenUntil precedent. Fix: hold the subject still and burn plies with a different piece, so the only variable is the clock.
- [2026-08-06] Same shape as a SAMPLE rather than a fixture: a contrast probe used Playwright `.first()`, which bound to the first matching square in DOM order — a rook, which renders correctly — so the one glyph that structurally could not pass (an emoji ignoring CSS color) was never measured. Fix: iterate every qualifying element and name the failing one in the message.
- [2026-08-26] Three A.5 rounds on one observer, each finding the same shape in a new place: played=>used with no held-but-unplayed control, moved=>used with no never-moved control, and no seen-negative at all — so an observe() returning every reachable id passed the file. Closed by sweeping every predicate rather than the two cited, using witnesses absent BY CONSTRUCTION (12 of 17 square types unpainted, 7 of 13 pieces unplaced) instead of ones a walk happened not to reach.
## [fail:design] built-but-not-wired | 2026-08-05 | count:1
An artifact that validates, is fully covered by tests, and is loaded by no code path is not shipped — but every count-based acceptance criterion says it is. The bundled content set (11 rule cards, 15 skill cards, 5 square types) passes its own AC's counts and a zero-error validation, while the app still imports the earlier throwaway slice, so no player can reach a single one of those cards. The tests were honest about what they measured and silent about what nobody asked them: the suite asserts the bundle is well-formed, never that the product loads it. The cheap guard is one assertion that the application entry point resolves to the shipped artifact — grep the import, or better, an e2e that reads a marker only the real bundle produces. Generalization: when an acceptance criterion is phrased "the MVP ships N of X", split it deliberately into "N valid X exist" and "the running product serves them", and write down which one the test actually covers. Discovered here as a deliberate, named deferral rather than a bug, because rewiring would have rewritten eight e2e specs pinned to the older content — but a deferral that is not written down is indistinguishable from an oversight three weeks later.
## [fail:test] gate-enumerates-one-axis-blind-to-others | 2026-08-05 | count:3
A coverage gate enumerated along one axis is blind to extensions along every other axis, and its greenness is read as "the vocabulary is fully supported". The gate here exists to catch an editor that cannot author some part of the content schema (ADR-006's permanent CI gate), and its first draft enumerated `axis:kind` pairs — 44 of them, complete against the schema's discriminated unions. It was still blind to the single most important extension the project had shipped: schema v3's headline addition was `duration`, a PARAMETER on three existing action kinds, not a new kind. An editor with no duration control would have passed a green 44-row gate while every skill card carrying one of those actions was a silent no-op — which is exactly the `declared-but-inert-vocabulary` failure the gate was built to prevent, reappearing inside the gate itself. The same blindness hid record-level fields with no other authoring route (`royal`, which decides what ends a match; `promotion`; `paired`; ADR-010's `pairedWith`; and `attack`, the field the project's signature piece is built on). Fix: every row carries the parameter controls it must expose, by test id, plus the exact value they must produce, asserted at the JSON path the control owns. Generalization: before trusting an enumeration-based gate, ask what SHAPES of change the thing under test can undergo, and check that the enumeration has an axis for each — a list of kinds cannot see a parameter, a list of fields cannot see a constraint, and a list of files cannot see a call site. The tell is a version bump whose changelog entry does not correspond to any new row in the gate.
- [2026-08-08] 2026-08-08: a generated-sequence property test, not an enumeration gate, and the same blindness. 200 pointer sequences swept displacement x hold x end-square x cancel x trailing-click and proved at most one of {inspect, tap, drag} per sequence -- but every case was ONE gesture, so the domain had no axis for what happens NEXT. Both P1s a second-opinion model found lived in exactly that gap: a drag armed a click-suppression flag that no click ever cleared, so the following Enter/Space was swallowed; and a second finger replaced the stored sequence, so an up from either resolved the wrong gesture. The shape to check is not only which VALUES the domain enumerates but which INTERACTION COUNT it enumerates -- a property over one interaction says nothing about state carried into the next.
- [2026-08-11] 2026-08-11: the enumerated axis was a NAVIGATION MECHANISM rather than a value set. A new e2e sweep built its coverage from page.locator(".tabbar .tab") and clicked each tab, which reaches four screens; the two CSS families the change squeezed hardest render nowhere a tab can go — .match-tools only inside a live match, .build-steps only inside the room builder — so the rules at highest risk from the change were the ones with no measurement, and the sweep reported clean. Both a cross-model voter and the ux reviewer found it independently, same file and line. The tell: the gate enumerated how you GET somewhere instead of what you must COVER, so a subject with no path through that mechanism is invisible by construction rather than by omission.
## [fail:test] assertion-equals-its-own-default | 2026-08-05 | count:7
A test whose expected value equals the implementation's own default never exercises the thing it names, and reads as full coverage. This is distinct from a missing negative instance: the fixture has a positive, drives the right control, and still proves nothing, because the asserted state was already true before the control was touched. Four review rounds on one file found it in three disguises — a whole-draft subtree search that matched an editor default (`condition: always`, `target: self`, `destination: chosen_empty` are all defaults, so a dead button passed); a trigger axis where the fresh object was seeded with the very trigger under test, and for a single-valued host (`skillCard` admits only `on_play`) there was no other value to switch to at all; and a parameter whose default came from `firstPiece(ctx)`, so asserting `to: 'piece.king'` against a set whose first piece IS the king proved nothing. Per-row hand-written discriminators fixed each instance and would have rotted on the next row added. The fix that holds is structural and belongs in the RUNNER, not the rows: before touching the control, assert the target path does NOT already hold the expected value. Two things that guard does not cover, and both bit — it fires once, so a SECOND control applied afterwards (a parameter following the main click) stays unguarded, and it cannot see a write that is idempotent with an earlier step's. What settles it is mutation: kill the control's write and confirm the suite goes red. Before the fix, killing either of two controls left 203 tests green while every authored promotion silently targeted the set's first piece. Generalization: for any "this control writes X" test the load-bearing fact is the PRE-state, and picking an expected value the implementation cannot produce by accident is cheaper than arguing that it would not.
- [2026-08-06] Same shape in a REGRESSION test rather than a control test: the asserted outcome was already true under the implementation being replaced, so the test could not go red for the change it named. Deletion now drops overlay text by reachability instead of by the deleted id prefix, and the case guarding it gave two records a shared key of `both.name` — which never matches the `piece.one.` prefix the OLD code keyed on, so the key survived under both implementations for unrelated reasons. Caught by the re-review, not by running anything. Fixed by moving the shared key INSIDE the deleted namespace (`piece.one.name`, pointed at by a surviving record), the one shape where the two implementations disagree. Generalization for regression tests specifically: state which input the old and new code answer DIFFERENTLY, and put that input in the fixture — a case both versions handle the same way documents intent and guards nothing.
- [2026-08-07] Third and fourth instances, both in guards written to protect an accessibility fix. (a) A Tab-trap test opened the real peek sheet, which has ONE focusable child, so first === last; jsdom does not move focus on a Tab keydown, so `sheet.contains(activeElement)` held whether the trap existed or not — deleting the whole handler passed. (b) The two-stop rewrite fixed that but still could not distinguish a correct boundary-scoped trap from one hijacking EVERY Tab, because with two stops every position is a boundary and "forward Tab from first lands on first" is true of both. Settled by a three-stop fixture asserting an interior Tab is left alone, and by running two mutants (trap disabled, trap hijacks everything) to watch each assertion go red. Generalization: a guard nobody has watched fail is a guard nobody has tested — mutate it once before believing it.
- [2026-08-08] 2026-08-08: the asserted node was the one node that could not move. AC-001 says selecting a piece must not reflow the layout; the reserved region is the hint bar, which sits BELOW the board, and the test asserted the boards y and height were unchanged. They were -- and would have been under any bug, because a region that grows downward cannot push the thing above it. The strip measured 85px against a 72px min-height reservation, so the legend and hotbar really were being pushed, and the assertion was true and vacuous. Found by measuring the region rather than by any test failing. Fix: assert the REGIONs own height across both states plus document.scrollHeight <= clientHeight, and make the reservation a fixed height with a clamp on the one row that can grow. Guard: when an invariant is about "nothing moves", assert on the element that would move first, not on the most convenient landmark.
- [2026-08-09] Twice in one task. tests/ui/art-refined-pieces.test.ts pinned its roster with toHaveLength(12) on a literal twelve-name array, which proves only that the array is the length of itself — a bundled piece added later would go unmeasured while every assertion stayed green (fixed by deriving the roster from artRegistry and the content set). And a viewBox test asserting "0 0 12 12" at SPRITE_SIZE=12 cannot distinguish a derived expression from a hardcoded literal — fixed by mocking the constant to 7, a value neither the old nor the new grid uses.
- [2026-08-09] Sixth instance, and the first found by a cross-model voter rather than by review or by running anything. The stampOf fixed-point test merged the bundle into ITSELF, so every candidate id was already in the saved set and the skip predicate short-circuited before it ever consulted the stamp: added === [] for any stamp, including one returning no ids. Confirmed by mutating stampOf to return { ids: [] } — the original passed, the rewrite (two merges over a saved set MISSING a bundled record, differing only in the stamp, with opposite outcomes) goes red. The file header stated the rule that forbids this.
- [2026-08-13] Seventh instance, and this unit produced it five separate times in the test-review gate, which is worth recording as a cluster rather than as one slip. Every one had the same shape: an assertion whose expected value was computed by the same mechanism the subject would use, so both sides moved together and the test could not fail. (1) `expect(rendered).toContain(t('ui.intro.turn.body'))` — the translator returns the KEY on a miss, so a component calling the right key with no bundle entry rendered the dotted key and the assertion compared that fallback to itself. (2) `expect(play.side).toBe(before.sideToMove)` — a card play never changes `sideToMove`, so reading `prev` or `next` was indistinguishable on every fixture the file could build. (3) a test written specifically to pin the absolute-expiry key could not distinguish it from the countdown, because `play_card` does not advance `plyCount` and the two encodings are numerically identical on every pair the function is handed. (4) an `effectKey` block that held square, sourceId and layer constant, so a key dropping all three passed. (5) an AI-timer test sampling at a moment outside BOTH the old floor and the new one, which passed against the unmodified source. The fixes divide into two kinds and the second is the useful one: where an independent oracle existed, use it (read the `ko` bundle directly and assert the entry EXISTS before comparing; derive expected squares from a different rendered attribute). Where none exists, force the divergence by hand — a fixture where `prev.sideToMove` and `next.sideToMove` actually differ, a sampling point that falls strictly between the two timer floors, one case per key dimension. And where neither is possible, say so in the test rather than shipping a comment claiming a discrimination the assertion does not perform: two of these were only found because a reviewer read the comment's claim against what the code could observe. A timer-id spy was also attempted here for a sixth case and abandoned — it installed after the timer it meant to watch was scheduled, so the set came up empty and the assertion passed vacuously, which is the same failure a third time.
## [fail:design] rule-keyed-to-event-not-state | 2026-08-05 | count:3
A terminal condition attached to the EVENT that usually causes it, rather than to the STATE it produces, is silently skipped by every other route to that state. The match-ending rule "capturing the king wins" was implemented as a short-circuit inside the capture step of the pipeline, which was correct while capture was the only way a king could leave the board. It stopped being the only way the moment the effect vocabulary gained a destroy action with a chosen target — and a shipped card does exactly that. A king DESTROYED rather than captured left no result behind, so play continued for another fifty plies with king-capture victory unreachable for BOTH sides, and neither the type system nor six phases of scripted tests noticed, because no hand-written fixture had ever played the line where a card kills a king. It was found on the third generated seed of a random-play invariant walk. The fix is the generalization: judge the STATE ("this side has no royal") rather than the event, and judge it as a TRANSITION — had one at the start of the ply, has none at the end. Both halves matter; the first attempt judged against the board's starting definition and wrongly condemned a legitimately royal-less puzzle position, which is exactly what the editor's preview builds. Two rules follow. When a win/loss/termination condition is written, ask what OTHER mechanisms can reach the same state and key the rule to the state. And when a vocabulary gains an action that can remove, create or relocate a piece, re-audit every rule written when fewer mechanisms existed — a new action does not announce which old invariants it just widened.
- [2026-08-08] Second instance, in the UI rather than the engine, found by a child asking where to click. Playing a card was keyed to the SQUARE-CLICK EVENT that usually completes one, not to the STATE "this card is complete" -- the only code calling push() for a card lived inside clickSquare. That is correct while every card names at least one square, and stops being correct the moment a card names none: a card that quantifies over your own pieces, or places one at your home rank, is offered by the engine as a play with an EMPTY target list. Those armed, highlighted nothing, refused every square the player tried, and said the card cannot target those squares -- accurate and useless, since it could not target ANY square and did not need to. Three of the fifteen shipped skill cards had this shape and only one was reachable at the opening, which is why it survived a full e2e suite: the other two want a home rank that is still full when a match starts, so nothing could reach the dead end until late game. Fix: compute the completed action from the pending state and render its commit, rather than waiting for the event that usually produces it. Two notes on the shape of the repair. Committing on the ARMING tap would have been the smaller diff and was wrong -- arming is what makes the detail panel explain the card, so a one-tap play spends the turn before a child has read what it does; the commit needs its own affordance. And the guard asserts the card DID something afterwards (a pawn can now rush two squares) rather than only that the turn passed, because unlocking the UI for a card that is still inert is a fix that measures itself. Generalization matching the engine instance: when an action is attached to the input that normally triggers it, enumerate the inputs that reach the same READY state by another route -- an empty selection, a zero-length list, a default that needs no choice -- and key the action to the state instead.
- [2026-08-11] A royal could inherit a still-live skill freeze or grant after moving onto its square because immunity was checked only when the skill initially targeted a piece; consuming persistent state now rechecks the current occupant.
## [fail:test] unasserted-fixture-premise | 2026-08-05 | count:1
A test states a premise about its own fixture and never checks it, so a green suite certifies an experiment nobody ran. Two instances landed in one phase, both caught by review before implementation. First: an acceptance criterion reading "1000 self-play matches played by a UNIFORM-RANDOM legal-action agent" was covered by a statistical suite that constrained the median and nothing else — an agent written as `legalActions[0]` is deterministic, terminates, and satisfies that whole file plus the determinism suite plus the invariant walk, while measuring the first-legal-move line instead of random play. The project's headline "a match is short" claim would then have rested on a degenerate fixture. Second: a bias test compared draft offers reached "down different lines" without ever asserting the lines differed, and the modular arithmetic generating them really could collapse two variants onto the same choices — in which case it compared a run to itself. The rule: whenever a test's Given names a PROPERTY of its fixture (uniform, distinct, sorted, empty, concurrent, different), that property needs its own assertion, either inline before the comparison or in a test of the fixture itself. Fixtures are code, and the ones that describe an experiment are the code most likely to be believed without evidence. Concretely, this phase added a test file for the agent (distribution flatness, no first/last index bias, reproducibility, null exactly when nothing is legal) and made the bias test return the position it arrived at so every comparison asserts the positions differ first — and count how many genuinely differing comparisons were made, so a fixture that silently stops varying fails instead of passing quietly.
## [fail:design] fix-scoped-to-the-cited-evidence | 2026-08-06 | count:7
A fix scoped to the evidence a critique happened to cite reproduces the very defect when the defect IS one of scope, and the second pass reads as progress because the first three files now check out. Concretely: a plan-validator flagged that a new PLAN's ADR-001..ADR-010 collided with ADR numbers already burned into the codebase, and named `schema.ts`, `Edit.tsx` and `Play.tsx` as its evidence. The renumber to ADR-013..ADR-022 was verified against exactly those three files and declared resolved — but `match.ts` already owned ADR-013 and `rng.ts` / `agent.ts` already owned ADR-014, so two of the ten still collided and re-validation returned MAJOR_REVISION a second time on the same finding. The critique's file list was an EXAMPLE of the collision, never an enumeration of it, and treating it as one preserved the failure while producing every appearance of having fixed it. Fix was `grep -rhoE "ADR-[0-9]{3}"` across the whole tree — 001 through 014 all taken — then shifting the block to 015..024. Generalization: when a finding is about a namespace, a scope, or a set, the remedy has to be derived from the whole set, and the cheapest correct move is a deterministic sweep (grep, a collected list, a schema query) rather than a re-read of the files the reviewer sampled. The tell is a critique whose subject is plural — "collides with numbers already in use", "breaks callers", "leaves entries unmigrated" — paired with a fix whose verification is singular. Corollary that applies to review loops generally: a second reviewer pass is not the strongest available check when the property is mechanically decidable; run the sweep and let the reviewer adjudicate what the sweep cannot.
- [2026-08-06] Non-review instance: `:root[data-theme=light]` was authored by copying the tokens the author was looking at and carried most of the dark branch, missing `--color-glyph-outline` and `--color-focus-ring`. A user on a dark OS who chose light kept a near-white glyph outline on a near-white board. An override block is a SET obligation — derive it from the branch it overrides, do not hand-copy.
- [2026-08-06] CSS instance, and the fix that caused it was itself a fix. The focus ring was moved from `outline` to `box-shadow` to escape being clobbered by the legal/selected rules — verified against those two, never against the full set of rules already setting `box-shadow` on `.square`. `.square[data-last]` was one of them at identical specificity, so tabbing onto the just-moved-to square erased the last-move ring. Moving a rule from property A to B escapes nothing until you grep every rule matching the same element that also sets B.
- [2026-08-06] Three times in ONE work unit, all in the phase-9a editor. (a) The rename repair re-derived `<id>.name` unconditionally, which handled the derived-key record that motivated it and silently re-homed a record keyed in a foreign namespace, stranding its overlay entry. (b) The stale-document guard compared only the stored RECORD, which its own motivating case defeats — an import that changes a name changes the overlay while the record, carrying only the key, is byte-identical. (c) The discard-confirm was bolted onto the room shortcut the finding cited and left the kind picker, the record list and the new-record button — the three more travelled paths to the same remount — unguarded. Each time the cited example was one member of a set the fix never enumerated; the cure is to name the SET the reported case belongs to before writing the guard.
- [2026-08-07] 2026-08-08: fixed the grade rig's skill axis (control identical to arm) and left the SAME shape on the piece axis, where the control was still the untouched preset and so inherited any pre-existing loadout. A cross-model reviewer found it. Worse, the fix I did make introduced a second instance on the axis I had just repaired — the corrected pools now differed in LENGTH, which re-randomized the draft. The cited evidence was one symptom on one axis; the defect was 'the control is not a control' everywhere.
- [2026-08-08] Measured "no shipped record is unshowable" against the BUNDLED content set only — the set the RESEARCH measurement came from — while the app loads three. A slice piece with two effects was the counterexample, so a save-block built on that premise made a shipped piece uneditable; the e2e suite caught it three phases later. The coverage test now sweeps all three sources and asserts the source COUNT, so a fourth cannot join silently.
- [2026-08-08] Twice in one unit. A refused move was wired to say why on the TAP path only, because a tap is what the report described; usePressInspect resolves one pointer sequence into exactly one of inspect/tap/drag, so the reported defect stayed alive on drag, and every authored test used fireEvent.click so none could see it. Then the stale-refusal fix was aimed at the press handler alone when six call sites open an inspection sheet. Both were found by review, one by two independent reviewers. The tell in both: the fix touched the code path named in the evidence rather than the set of paths that reach the same commit.
## [fail:design] comment-claims-unbuilt-safeguard | 2026-08-06 | count:10
A comment that states a safeguard is more durable than the safeguard, so a design invariant written in prose and implemented in only one branch reads as satisfied everywhere and is trusted by the next reader. Concretely: a new design-token file opened with "the two sides are separated by hue AND by weight, not hue alone — roughly one in twelve boys reads red/green poorly", and neither half of that was true. No selector anywhere set a per-side `font-weight` — the weight cue existed only in the sentence claiming it. And the dark palette, authored in the same file below that sentence, gave the two sides `#93c5fd` and `#fca5a5`, whose relative luminances are 0.540 and 0.545 — indistinguishable in lightness, leaving exactly the hue-alone dependence the comment forbade, while the light palette a few lines earlier did carry a real lightness gap. Two independent reviewers found it; one read the comment against the stylesheet and called it documentation drift, the other computed the luminances and showed the invariant was actively violated in one theme. Fix was to implement rather than retract: a per-side font-weight in the component CSS, a dark side-colour with a real lightness gap that still clears 4.6:1 against the board, and a comment that now says where the weight half lives and that review found it claimed before it existed. Generalization: a comment asserting a PROPERTY (accessible, deterministic, thread-safe, idempotent) is a claim that needs a test, not documentation — and the specific shape to distrust is an invariant stated once at the top of a file whose body then branches, because the statement is written while looking at the first branch. The cheap guard is to write the property as an assertion in the same commit as the sentence; where that is genuinely hard, phrase the comment as intent ("should") rather than as fact ("is"), so the next reader knows to verify rather than rely.
- [2026-08-06] Phase 8: commitDraft carried a comment promising a stale save "appends rather than silently dropping" — it appends only when the typed id is free; on collision it replaced an unrelated record. The safeguard existed in the sentence and in one branch.
- [2026-08-07] Third instance, and inside the fix for a second instance. The round-2 fix for the room builder guarding one of its two records added a board stale-check whose comment read "a fork writes a new record, which cannot be stale by construction" — while the code ran the check before forkBoard had decided anything, so every new room (seeded from a shared boards[0], hence always forked) was refused when that seed board had been edited elsewhere. The comment described the correct design; the code implemented the check unconditionally. Fixed by moving the gate after forkBoard and conditioning it on boardId === openedBoardId, and pinned by a test that fails when the gate is moved back.
- [2026-08-07] 2026-08-08: src/balance/cache.ts opened with 'editing a record changes its key... there is no path where an edited record keeps the grade its previous version earned' while keyForRecord hashed the baseline BY ID. Editing the reference piece every other piece is graded against, or the room's board or rule pool, left every other record's key unmoved and its cached grade silently stale — exactly such a path. Written in the same file whose comments cite the neighbouring declared-but-inert failure class. Fix: resolve and hash the reference records, the preset and its board by value; six tests each edit something OTHER than the keyed record and require the key to move.
- [2026-08-07] 2026-08-08: twice in one AI-search commit, and BOTH comments were written while looking at the branch that was true. src/engine/ai/engine-side.ts said 'the valve REPORTS an overrun; it does not cause one' while ADR-010 promised it returns best-so-far — so the rejected alternative 'accept the risk, the AI can hang the tab' was quietly in force. And engine.ts said of TrustedAction 'the one mint is the cast at the end of legalActions... keeping it to a single expression is what makes it auditable' while search.ts held a second 'as TrustedAction'. Both were safe by adjacency the day they were written and neither had a test. The tell that generalizes: a comment that argues WHY the safeguard is enough is the shape to distrust — it is written to persuade a reader, which is what you do when you are not asserting it in code.
- [2026-08-08] The piece grid commit handler returned early when writeGrid rejected an empty grid, with a comment saying the piece-no-moves note would explain the refusal — but noMoves is computed from the same unchanged grid, so the note could never render and tapping a blank piece one seeded cell did nothing at all.
- [2026-08-08] Seventh instance, in CI config rather than code: .github/workflows/deploy.yml installed chromium only and justified it with "both configs declare a single mobile-portrait project on devices[Pixel 7]" — true when written, false the moment a mobile-webkit project on an iPhone 13 was added without touching that line. Every webkit test then failed with browserType.launch: Executable does not exist, which reads as sixty broken tests rather than one missing download. The general shape: a comment that asserts a fact about ANOTHER file is a claim with no owner, and the other file is free to change. Fixed by listing the engines explicitly and rewriting the comment to say where to look when a launch fails, rather than what the config currently contains.
- [2026-08-08] Twice, and the second is a new layer. RecordGrade.tsx still described a Web Worker, a measuring state and a content-hash cache that ADR-012 deleted, in a file whose neighbour useGrades.ts says the opposite. Worse: ui.editor.loadout.caveat told the PLAYER the value was measured by having a computer play many random games — the deleted self-play rig, in player-facing text, printed directly under the new term-by-term arithmetic and contradicting it. Prose outliving its mechanism is not only a code-comment failure; a translation bundle is prose too, and nothing scans it.
- [2026-08-09] Ninth instance, and I wrote the comment myself two stages earlier. `src/ui/recordLabel.ts` documents `recordLabels` with "every surface that renders more than one record at a time goes through here rather than calling `recordLabel` per row" — and eleven picker and grid sites across `RoomDetail.tsx`, `RecordForm.tsx` and `MakerGallery.tsx` called `recordLabel` per row inside a `.map`, with no ordinal. The two list screens I had in mind when writing the sentence did honour it, so the file read as satisfied. Consequence: two records that both fail to resolve a name render as the same three words on every picker, distinguishable only by DOM position — the disambiguation the ADR promised existed on two screens out of thirteen. The paired test defect is the same shape: the "tellable apart" assertion was written against `EditorRooms` only, so the invariant was checked exactly where it was announced. Fixed by making the code match the sentence rather than lowering the sentence, plus an assertion on the picker surfaces that was mutation-checked (reverting the `RecordForm` half fails it and nothing else). Guard for the family: a doc comment saying "every X goes through Y" is a claim with a grep behind it — `rg` for the bypassing call and count the hits before writing the sentence, because the sentence will outlive your memory of which branches you actually converted.
- [2026-08-13] Tenth instance, and the safeguard was a CSS comment asserting an overlap could not happen while the feature's own ADR guaranteed it could. The impact ring was written as `.square[data-impact='true']::after` with a comment reading: a square being chosen as a move target while a card's ring is still running is a state the ordered notice pick makes unreachable, because the ring belongs to the ply that just ended. The legal-move dot is `.square[data-legal-kind='move']::after` — same specificity, declared earlier, so the ring wins on any square matching both. And the two DO coincide: ADR-005 of the same PLAN deliberately leaves the human path ungated precisely so the player can pick the move their card still owes while the banner is up, and a square a card just freed is exactly what the next move targets. So the move affordance was silently replaced by a ring during the one moment the whole feature exists for. Nothing failed: typecheck, 873 unit tests and 427 e2e were green, because no test had ever put a legal-move target and an impacted square on the same cell. Found by the code reviewer reading the comment against the ADR it cited. The tell is specific and worth reusing: the comment argued unreachability from a DIFFERENT subsystem's behaviour ("the ordered notice pick") than the one that decides it (`reachable`, which is computed from the selected piece and knows nothing about notices). An unreachability claim is only as good as the code that enforces it, and if that code lives in another module the claim is a hope. Fix was to stop competing for pseudo-elements at all — the ring is a child element now, since `[data-last]` already owns `::before` and the dot owns `::after`, so relocating would only have moved the collision — plus an e2e that selects the piece owing its move while the ring is up and asserts the dot is still drawn.
## [fail:test] test-setup-hides-the-failure-path | 2026-08-06 | count:6
A test that grants, mocks, or stubs the very condition whose absence is the risk certifies the happy path and makes the dangerous branch permanently unreachable — and its greenness is read as "this is covered". Concretely: a copy-the-seed button was written as `void navigator.clipboard?.writeText(x); setCopied(true)`, reporting success unconditionally, so on an insecure context or an older mobile browser — where `navigator.clipboard` is undefined or the promise rejects — a child taps the button, reads the success label, and shares nothing. The e2e test written for that button opens with `context.grantPermissions(['clipboard-read', 'clipboard-write'])`, which guarantees the API is present and permitted, so the only branch it can ever exercise is the one that works. The reviewer found it by reading the code, not by running anything; no test could have. Fix: let the promise decide (`copied` / `failed` / `idle`), give failure its own visible label, and expose the state on an attribute so a test can assert it. Generalization: whenever a test's SETUP mentions the same capability the code under test is optional about — permissions granted, a network stubbed as available, an env var set, a feature flag forced on, a file created that production may not find — the absent case has no coverage by construction, and that is precisely the case where the code is most likely to lie. The cheap guard is to write the negative test first, or at minimum to name the missing branch in the test file so the gap is visible rather than implied. Related in shape to [fail:design] built-but-not-wired: the artifact is honest about what it measures and silent about what nobody asked it.
- [2026-08-06] Third form, and the harness supplied the missing capability this time rather than a permission: drag was built on HTML5 drag-and-drop, which iOS Safari never dispatches for a finger, and the e2e passed because Playwright's dragTo synthesises MOUSE events. The suite certified a gesture no player on the target device could perform. Fix: pointer events in the component, and mouse.down/move/up in the test so it raises the same pointer events a touch does.
- [2026-08-07] Two e2e about a discard-confirm dialog had never seen one. `inProgress` is `states.length > 1`, so a freshly started match has nothing to lose and no confirm fires — and both outcomes asserted (board kept / board gone) are what happens with NO guard at all, so both passed. Fixed by making a draft pick first AND by counting dialogs, so the prompt existing is itself asserted.
- [2026-08-07] 2026-08-08: a test asserted the exact condition that made a whole axis dead. The grade rig measured a skill card by adding it to the loadout; every bundled card is already in the shared pool and skillPoolFor deduplicates, so arm and control were identical and all fourteen graded 0.00 +/- 0.00. The test written for it pinned that as CORRECT — 'a card the pool already deals measures zero' — and the surrounding tests only checked the numbers were finite. The suite therefore certified an inert axis. Found by printing the distribution, not by any assertion.
- [2026-08-07] 2026-08-08: every test for a new picker added a record of its own to the fixture before mounting, which made the DEFAULT state unreachable by construction. The loadout picker offers skill cards a side may own, and a card the room deals to everyone cannot be one — correct rule. The shipped room deals all fifteen cards, so the shipped experience is a menu containing only "안 가져가요" and no sentence explaining it: a control a child cannot complete. Six tests covered the picker and none could see it, because each one pushed a probe card outside the shared pool as its first line. The fixture was not wrong for what each test asserted; it was wrong as the ONLY fixture. Found by opening the app in a browser. The guard: when a screen's content is derived by subtracting one set from another, one test must mount the SHIPPED document unmodified — the subtraction coming out empty is the case the product actually ships, and a fixture that adds an element to make the test convenient deletes exactly that case.
- [2026-08-09] Sixth instance, with the optional input being a STORAGE KEY. The test proving initialSource writes nothing sat inside describe("a saved install with no stamp") and SEEDED a stamp, so it proved "no write when a stamp exists" and left the absent-stamp branch — the state of every install alive today — with no no-write coverage. The mutation it cannot catch is the design risk R1 itself: write the synthesised stamp only when loadStamp returns null, and the release additions land in the stamp but never in the document, so the next load reads them as deletions and drops them. Injected that mutation: the corrected no-stamp test fails, the stamp-present test passes — which is exactly the asymmetry that made the original blind.
## [fail:design] counter-as-proxy-for-a-state | 2026-08-06 | count:1
A counter that sits near the thing you mean is not the thing you mean, and substituting it produces a guard that is silent in exactly the state it exists to protect. Concretely: a confirm-before-discard gate was added to a hot-seat game after review found that a mis-tap on the always-visible new-match button destroyed a shared in-progress match with no way back. The gate asked `state.plyCount > 0` — "has anyone moved yet" — as its proxy for "is there a match worth keeping". But a draft pick does not advance the ply counter in this engine, so a match in which BOTH players had already chosen their skill cards reported nothing-to-lose and was discarded without a prompt: the single state where a mis-tap costs the most was the one state the guard ignored. The predicate that actually means what it says is `match.states.length > 1` — one applied action of any kind. Two things made this findable. The proxy was wrong in the safe direction for the tests that already existed (fresh mounts have neither plies nor actions, so nothing changed), and the ONLY reason it surfaced is that a test for the new guard was written immediately after the fix and failed against it. The fix had otherwise shipped with no coverage at all, which is the deeper error: a guard added without a test that exercises the guarded state is indistinguishable from no guard the moment the predicate drifts. Generalization: when a guard's condition is a counter, ask which state transitions increment it and list the ones that do not — the answer is the set of cases your guard is blind to. Prefer a predicate over the state you actually mean (was an action applied, does a document exist, is the buffer non-empty) over an adjacent metric that usually correlates.
## [fail:design] related-ops-fail-independently | 2026-08-06 | count:1
Two operations on the same resource are assumed to fail together, so guarding one is written and read as guarding both — and the unguarded half is exactly where the interesting failure lives. Concretely: a "has this player seen the tutorial" flag wrapped `getItem` in a try/catch that returned "already seen" on throw, with a docstring explaining at length that a private-mode browser throws and that failing this direction is better than replaying the tutorial forever. The docstring was wrong about the mechanism: a storage with a zero quota answers `getItem` perfectly well — returning null, because nothing was ever stored — and throws only on `setItem`. On that browser the code produced not-seen on every load and an unrecordable dismissal every time, which is precisely the replay-forever loop the comment claimed to prevent. The fix is not another try/catch: when the flag is absent, PROBE whether it could be written (`setItem` a throwaway key, `removeItem` it), and treat a storage that cannot keep the answer as having already given it. Generalization: for any read/write, connect/send, open/lock, acquire/release pair, ask what happens when one side works and the other does not — the asymmetric case is usually the real-world one, and a comment asserting they fail together is a hypothesis nobody tested. The tell here was that the existing test threw from BOTH methods, which mirrored the assumption rather than challenging it; a test fixture built from the same belief as the code cannot falsify it.
## [fail:design] phase-scope-omits-wiring | 2026-08-06 | count:4
A plan's per-phase scope lists the files the phase CREATES and omits the ones it must touch to make them reachable, so every phase reports scope drift for work that was never optional. Three phases in a row of one UI plan, same shape each time: Phase 1 (design tokens) listed the token file and the stylesheet, and spilled into `main.tsx`, because a token file nothing imports styles nothing. Phase 2 (a home screen and match lifecycle) listed three components, and spilled into seven e2e specs — one shared helper that walked a tab to the board took thirteen tests down the moment a screen appeared in between. Phase 3 (a rules screen and coach marks) listed its two new components, and spilled into the previous phase's `Home.tsx`, because a screen with no door is unreachable, and into the stylesheet, because the phase before it had shipped four class names with no rules. The drift verdict was `scope_violation` all three times and the work was correct all three times, which is the tell: when the gate fires on every phase and the answer is always "necessary", the scope list is the thing that is wrong, not the implementation. What to write instead of a file list: the entry point that must resolve to the new thing, the tests that assert the surface being moved, and the previous phase's files that must hand off to it. Generalization beyond planning — any "files changed" estimate made by listing artifacts rather than tracing reachability will be short by exactly the wiring, and the wiring is where the integration bugs are.
- [2026-08-06] Fourth instance, same PLAN. Phases 6b and 7 named outcomes (safe area, breakpoints, trays, manifest, SW) and omitted the files carrying them — MatchHost.tsx, App.tsx, ko.ts, package.json, plus the second Playwright config and the icon script the work required. Seven files landed outside any scope list and the drift gate returned scope_violation. Phases 4 and 5 re-derived scope by tracing reachability and are still the only two in this plan whose drift came back clean; 6b/7 did not use that method.
- [2026-08-06] Phase 8: 12 files outside scope — 5 UI components carrying the translate signature change the PLAN itself priced as "touches every rendering call site", bundled.ts forced by an existing version assertion, and 6 stale imports. Fifth occurrence in this one PLAN.
- [2026-08-09] PLAN-art-grid-resolution Phase 1 exit criterion required its test to call upscale(), but scripts/upscale-sprites.ts appeared only in Phase 2 scope — Phase 1 could not satisfy its own criterion without touching a file another phase owned. Three validator passes missed it; found at execute time and recorded as a scope correction rather than silently absorbed.
## [fail:render] glyph-opts-out-of-its-styling | 2026-08-06 | count:3
A value that looks like its siblings but is drawn by a different rendering subsystem silently opts out of every style rule the siblings obey, and the code reads as if it obeys them. Concretely: a chess board drew five pieces as Unicode chess glyphs (♚♛♜♞♟) and the sixth — the variant's signature piece — as `🏹`. Sides were distinguished by `color` plus a `font-weight` cue that the token file explicitly documents as a colour-blind safeguard. `🏹` is in Unicode's default-emoji-presentation set, so browsers draw it from a COLR/CBDT colour font with its own embedded palette: `color` is ignored, `font-weight` is ignored, and both archers rendered pixel-identical for the two sides. Nothing in the CSS, the component or the content hinted at it — the six entries sit in one list in one file, five text and one emoji, and the difference is invisible until it is on a screen. Two reviewers found it independently, one by tracing the cue contract through the stylesheet and one by asking what a child would see, and it was the first cross-reviewer agreement of the project. Fix was to pick a glyph in the same monochrome family; the accepted cost is that it carries a different chess connotation. Generalization: whenever one member of a styled set comes from a different technology — a colour emoji among text glyphs, an `<img>` among icon fonts, an iframe among DOM nodes, a canvas among SVGs — assume it inherits nothing and verify the specific properties the design depends on, because the failure is silent everywhere except the pixels. The tell is a set that is uniform in the source and heterogeneous in origin. The companion failure is the test: the one probe written for this defect sampled `.first()`, which bound to a piece that renders correctly, so the only glyph that could fail was never measured.
- [2026-08-06] Same shape in the UA layer rather than a font: every button/select/input/textarea rendered black-on-dark (~1.2:1) in the dark theme, because form controls do not inherit `color` — the UA substitutes `buttontext`. styles.css had set `background` since Phase 1 and never `color`, so the pair was half-styled for six phases and the light theme hid it. The contrast e2e could not see it either: it measures board TOKENS, never a control computed style.
- [2026-08-06] Third instance, and the generalization written here already named it: "an `<img>` among icon fonts". Board marks became `<img>` elements when raster art landed, and both wrappers that carry a mark declare their legibility ring with `text-shadow` — `.piece` in `--color-glyph-outline`, `.square-mark[data-occupied='true']` in `--color-cue-lo`. `text-shadow` is a TEXT property and does nothing to a replaced element, so the rule stayed in the stylesheet, kept passing every existing test, and produced no ring at all. It bites hardest in the state nothing exercised: a piece standing on a painted square shrinks the mark to a 12px corner badge, where an outline baked into a 192px asset is sub-pixel anyway, so BOTH the CSS ring and the asset's own ring were gone at once. Two reviewers converged on it from different directions (one tracing the metric, one asking what a child sees) — the same convergence signature the two prior instances had. Fix: `filter: drop-shadow(...)` four times, which rasterises against the image's alpha, declared in CSS pixels so it does not shrink with the art; added for `.piece` at the same time even though piece art does not exist yet, because the art batch is where it would be forgotten. The new lesson on top of the existing one: when a set member changes technology, grep the stylesheet for the properties the OLD technology consumed — `text-shadow`, `color`, `font-weight`, `letter-spacing`, `-webkit-text-stroke` are all silently inert on `<img>`, `<canvas>`, `<svg>` and `<iframe>`, and the CSS keeps parsing so nothing anywhere reports it.
## [fail:test] distinct-is-not-distinguishable | 2026-08-06 | count:1
Asserting that two values DIFFER proves nothing about whether a person can tell them apart, and the gap between the two is where a whole feature can ship invisible. Concretely: a chessboard's checker pattern was added, with a test asserting the two square colours were not the same string. They were not: `#f2efe9` and `#ded5c4`, which is a contrast ratio of 1.27:1 — a board a child reads as flat. The feature was "added", the test was green, and the deliverable did not exist. The same project had already recorded the identical shape one file away: a painted-square gradient sitting at 1.03:1 against the plain square while its stylesheet comment declared it "distinguishable at a glance". Fix: assert a luminance step, not inequality — compute the WCAG ratio in the browser and give it a floor. What the numbers then forced is the second half of the lesson: a 3:1 checker drives the tinted glyphs standing on those squares below 3:1 themselves, so the two requirements trade on one axis and the honest output is a measured table plus a stated choice, not a silent pick. Generalization: for any property a HUMAN has to perceive — colour difference, animation duration, font size, audio level, latency — the assertion must carry a magnitude and a threshold. `a !== b`, `toBeTruthy()`, "the class is applied", "the element exists" are all statements about the code's intent, and intent is exactly what is never in doubt. Ask what the smallest version of this that a user could NOT perceive would look like, then check the test still fails on it.
## [fail:design] api-absent-on-the-target-platform | 2026-08-06 | count:1
An API that compiles, has types, and runs in the developer's browser can still be absent on the platform the product actually ships to, and nothing in the toolchain says so. Concretely: drag-to-move was built on HTML5 drag-and-drop — `draggable`, `dragstart`, `dragover`, `drop`. It type-checks, it is in every reference, and it works in a desktop browser. iOS Safari does not dispatch those events for a touch gesture on a generic element, and Android's support is inconsistent, so on the only platform this product targets — two children sharing one phone — a finger on a piece did nothing at all. The fix was pointer events, which cover mouse, touch and stylus through one path. Two independent reviewers found it, one by checking the API against the platform and one by asking what happens when a child touches the board; it was the second cross-reviewer convergence of the project, and both times the converged finding was the most severe of its round. Generalization: for any browser or runtime API, the question is not "does this exist" but "does this exist on the device in the user's hand", and the two answers differ most often for input (drag, hover, contextmenu, vibrate, clipboard), for media, and for storage. A quick check is whether the API predates the platform's dominant input method — HTML5 DnD was designed for a mouse in 1997 and touch was retrofitted around it, never into it. The companion failure is that the test suite said yes: see the occurrence note added to `test-setup-hides-the-failure-path` on the same day.
## [fail:render] fixed-overlay-blocks-what-it-only-dims | 2026-08-06 | count:2
A full-viewport fixed layer added to DIM the page also intercepts every pointer on it, and a fixed sheet covers the bottom of the viewport at every scroll position, so an overlay written as a visual effect quietly becomes a modal trap plus an unreachable page tail. Concretely: the card draft moved out of the document flow into a bottom sheet over a `position: fixed; inset: 0` scrim so it would stop pushing the board off the phone. The scrim was purely decorative in intent — it only sets a background — but it sat above everything, so the nav tabs, the home button and the whole tools row went behind glass the moment an offer appeared. The e2e suite reported it as twenty failures with `<div class="draft-scrim"> intercepts pointer events`, which made it look like a test-fixture problem; the player-facing version was worse than any test, because a child who opened a match and did not want to draft had no way out except reloading. Fixing pointer capture then exposed the second, quieter half: the sheet is FIXED, so it covers the bottom of the viewport no matter how far the page is scrolled, and the tools row underneath it could not be reached by scrolling either. Fix: `pointer-events: none` on the scrim with `auto` restored on the sheet itself, plus a `:has()`-scoped `padding-bottom` on the page for exactly as long as the sheet exists, so the page can scroll its own tail above it. Generalization: adding a fixed full-viewport element changes two contracts at once — hit-testing and reachability — and neither is visible in the property you wrote. If the intent is "dim", declare `pointer-events: none` in the same edit as the background, and give back `auto` only on the thing meant to be interactive. If the intent really is modal, that is a decision about whether the user can leave, so it needs an explicit way out, not merely an overlay. And any fixed element anchored to an edge steals that edge from the scrolling content behind it: reserve the space, do not assume scrolling reaches past it.
- [2026-08-06] Third contract that position:fixed drops silently — stacking. Two notice banners were moved out of flow to stop them pushing the board down mid-match; their conditions are independent (a content bundle that failed to load and a pending build ship together), so both rendered at the same fixed coordinates and the later one painted over the other, which became invisible and unclickable. Document flow had been stacking them for free. Fixed: one positioned .notice-stack, notices as normal blocks inside it, z-index above the draft scrim.
## [fail:render] contrast-set-not-closed-over-cues | 2026-08-06 | count:2
A measured contrast suite proves only the pairs it enumerates, so a colour changed to satisfy one measured constraint can silently collapse a different cue that shares the same surface — and the suite stays green because that pair was never a row in it. Concretely: Phase 6a repainted the special squares green to clear a measured "painted vs plain >= 3:1" floor, and `e2e/contrast.spec.ts` verified exactly that, in both themes, with real luminance maths. What no row covered was `--color-legal` (also green) sitting on top of that new fill: ~1.4:1 light, ~1.37:1 dark. The single cue that tells a player "you may move here" all but disappeared on precisely the squares whose ability makes the decision hard, and the suite that exists to prevent this reported success. The companion defect is semantic rather than numeric: green conventionally means go/safe, so the most dangerous square on the board — destroy-anything-that-enters — was the one wearing the reassuring colour. Fix had two halves, and the second is the durable one: the fill moved to violet (no collision, no wrong connotation), AND the legal cue stopped depending on a single colour clearing every fill it can land on — it is now a dot inside a black/white ring whose two tones are theme-INVARIANT, so whatever the square is, one tone separates from it. Generalization: a contrast table is a set of PAIRS, and the set is only honest if it is closed over every pair of things that can occupy the same pixels — foreground cues over every background they can land on, not just background-vs-background. When you add a colour or move an existing one, the question is not "does it still clear its own row" but "which pairs does this colour now participate in that have no row". The cheap structural guard is to stop making a lone hue load-bearing: a cue sandwiched between two invariant extremes needs no row at all, because it cannot be washed out by a fill it does not touch.
- [2026-08-06] Second instance, one round later. Dark `--color-selected` was moved off `#93c5fd` to break a three-cue hue collision; measuring the replacement showed NO single colour clears 3:1 against all four dark fills a selection ring can land on (amber 7.29 on the dark tile / 1.58 on the painted; white 12.17/2.63; near-black 1.52/7.04). A candidate winning at one end of the luminance range and losing at the other is the signature of a cue that needs two tones, not a better hex — sandwiched it between the cue-lo/cue-hi pair like the legal dot.
## [fail:render] sticky-inert-under-overflow-ancestor | 2026-08-06 | count:1
`position: sticky` is silently inert when any ancestor has an `overflow` other than `visible`, and `overflow-x: hidden` is such an ancestor even though nothing about it looks vertical. CSS computes a used `overflow-y` of `auto` whenever the other axis is not `visible`, so a container written years earlier as `main { overflow-x: hidden }` — added to stop a wide element scrolling the page sideways — quietly becomes the scroll container that a later sticky element positions against. That container does not scroll internally (it has no height constraint; it grows), so the sticky never activates and there is no error, no warning, and nothing in the computed style that reads as wrong: `position` still says `sticky`. Concretely: a landscape layout put the board in a grid column and made it sticky so a tall neighbouring column could scroll past it. Measured after the change, scrolling a 790px document in a 412px window left the board at **-293px** — entirely off screen, exactly as before the fix. A second guess (`align-self: start`, on the theory that a row-spanning grid item fills its area and has nowhere to slide) changed nothing, because that was not the cause either. The fix was `overflow-x: clip`, which clips without establishing a scroll container; after it the same measurement put the board fully on screen with horizontal scroll still zero. Generalization: when a sticky element does not stick, walk every ancestor for `overflow` on EITHER axis before touching the element itself — `hidden`, `auto`, `scroll` and `clip` are not interchangeable here, and only `clip` preserves stickiness. The wider lesson is that `overflow-x: hidden` has a vertical side effect that its name denies, so it is worth writing down wherever it appears. And the meta-lesson: two plausible CSS explanations were tried and both were wrong; the thing that settled it was measuring the element's box after a real scroll in a real browser, which took less time than either guess.
## [fail:design] plan-contradicts-its-own-adr | 2026-08-06 | count:1
A PLAN can contradict itself, and when a phase's scope line and an ADR in the same document disagree, implementing the scope line is the natural mistake because that is the part being executed. Concretely: ADR-018 recorded "keep AC-017's model — both hands, one board, both trays always visible", and the scope of a later phase in the SAME plan listed "card-shaped collapsible trays". Those cannot both hold. The implementation followed the scope bullet, because that is what an execute stage reads, and shipped a collapse that unmounted the cards — so a tap on the opponent's label hid their hand in a game whose whole premise is that both hands are open, and a player who left their own tray shut could not play a card on their own turn, because the button they needed did not exist. Nothing caught it: the phase's own exit criterion asked only that collapsing not reflow the board, which the broken version satisfied perfectly. A reviewer found it by reading the ADR that sat three sections above the scope it contradicted. Fix was to make collapsing CONDENSE rather than hide — cards stay mounted, only their prose is hidden — which satisfies both the ADR's constraint and the gap the scope line was chasing, plus a test that asserts the card count survives a collapse. Generalization: a scope line is a task, an ADR is a constraint, and constraints outrank tasks. Before implementing a scope bullet that changes what a user can see or do, grep the plan's own ADR section for the nouns in that bullet — here, "tray" and "visible" would have found it in seconds. The tell is a scope item phrased as a UI affordance ("collapsible", "hidden by default", "behind a toggle") in a project whose ADRs constrain what information must remain available; those two vocabularies overlap and the document will not flag the collision itself.
## [fail:render] hidden-subtree-took-the-a11y-name | 2026-08-06 | count:1
`display: none` removes a subtree from the accessibility tree, so a control whose accessible name lives only inside that subtree becomes a nameless button the moment the subtree is hidden — and it announces as "button", which is worse than the state the hiding was meant to improve. Concretely: a card in a player's hand rendered as a button containing an icon span marked `aria-hidden="true"` and a body span holding the card's name and description. A collapse feature then hid the body with `display: none` to condense the tray. The visual result was fine — the icon still identified the card — but the accessible name computation was left with an explicitly-hidden icon and a removed subtree, so every card in a collapsed tray had no name at all for a screen reader. The e2e written for that same feature asserted the card count and that the first card was visible; neither says anything about a name, so the regression was invisible to a green suite. Fix was `aria-label` on the button itself, so the name survives whatever happens to the body, plus an assertion on that attribute. Generalization: whenever a feature hides part of a control — a disclosure, a condensed mode, a responsive breakpoint that drops a label, a truncation that swaps text for an icon — ask what the control's accessible name was computed FROM, and whether that source is what just got hidden. The two shapes to distrust are an `aria-hidden` icon paired with visible text (hide the text and nothing is left) and a visually-hidden label class swapped for `display: none` (which is not the same thing: `display: none` hides from AT, a clip-rect utility does not). The cheap guard is to put the name on the control rather than in its children, so that presentation changes cannot take identity with them.
## [fail:design] clearance-inherits-the-suites-blind-spot | 2026-08-06 | count:1
A reviewer that validates code against its test suite inherits every blind spot that suite has, and reports the inheritance as a clearance — which is worse than silence, because the next reader reads "checks out" as evidence rather than as a restatement. Concretely: `commitDraft` gained an optional `openedId` so a rename would replace a record instead of appending a duplicate. A code reviewer returned zero P0/P1 and wrote that "the `matchId` fallback, the stale-`openedId`-append path, and base immutability on both the accept and reject paths all check out against the seven cases in `tests/editor/rename.test.ts`." Every word of that was true. None of the seven cases had an id COLLISION, so the suite was structurally incapable of reaching the defect: when `openedId` named a record the document no longer had AND the author had typed an id already belonging to a different record, the fallback matched that other record and the save REPLACED it and validated — silent destruction of an unrelated record, reported to the author as success. A cross-model reviewer, given the same diff and no access to the first verdict, named it directly. The disagreement was settled by neither: the case was EXECUTED, and the probe printed `ok = true` with the victim's movement overwritten. Fix: `openedId` alone decides which record is replaced (`findIndex(r => idOf(r) === (openedId ?? id))`), so a stale id matches nothing, the record is pushed, and the loader refuses the document with `duplicate id`. Generalization: "I checked it against the tests" is a claim about the tests, not about the code, and it is strongest exactly where it is emptiest — a suite written by the same person who wrote the code shares that person's model of which cases exist. The cheap guard when a reviewer clears a specific path is to ask which test would FAIL if that path broke, and to name it; if the answer is a suite rather than a case, the clearance is unearned. Corollary observed the same round: when the re-review then cleared a second path "by inspection", writing that case took two minutes and converted the confidence into a measurement. It passed — which is a different fact from the one inspection offered.
## [fail:tooling] add-all-swallows-a-nested-checkout | 2026-08-06 | count:1
`git add -A` will happily stage a nested git checkout as a gitlink, producing a commit that looks complete and contains none of the nested content — and the workflows that create such checkouts do not gitignore them for you. Concretely: the `/hm:` stages create git worktrees under `.worktrees/`, which was absent from `.gitignore`. A stale `.worktrees/augmentchess-quality-parity/` from an earlier task therefore sat untracked, and the `git add -A` that every wrapup runs — itself the fix for an earlier failure where staging only indexed paths truncated a commit — swept it in. Git printed `warning: adding embedded git repository` and continued; the exit code was 0, and the file count in `git status --porcelain` went from 25 to 26 with nothing obviously wrong. Had it committed, a clone of the outer repo would have carried a gitlink pointing at a commit nobody can fetch. Fix: `git rm --cached .worktrees/<name>` to unstage, then add `.worktrees/` to `.gitignore` so the trap is disarmed rather than merely dodged this once. Generalization: any directory a tool CREATES inside the repo — worktrees, vendored checkouts, build caches, virtualenvs, agent scratch dirs — must be gitignored at the moment the tool starts using it, not the first time it bites, because the blast radius is a commit and the warning that precedes it is a `warning:` on stdout in the middle of a stage that prints a lot of other things. When a stage adds a new such directory, the same change should add the ignore line. The confirming signal is cheap: after any `git add -A`, read the `git status --porcelain` list rather than its count.
## [fail:design] mount-snapshot-vs-live-dependency | 2026-08-06 | count:1
A component that snapshots state at mount while reading one of its inputs LIVE has a window in which the two describe different worlds, and every write it makes in that window silently reverts someone else's. Concretely: the editor's `RecordForm` / `RoomDetail` capture their draft and the id they opened under in `useState` initialisers, but `save()` closes over the `source` prop, which React re-reads on every render. Their `key` does not include `source`, so replacing the whole document — which is exactly what the transfer controls' export -> edit -> re-import round trip does — does NOT remount them. A save then matched the stale mount-time `openedId` against the NEW document and overwrote a record the form had never seen, with both the import and the save reporting success. The full suite stayed green because nothing exercised "import while a form is open"; `rg editor-import` found the control used three times and the one save after it followed a `page.reload()`, which remounts everything. Fix: snapshot what the form is responsible for not clobbering (`{record, name, text}` for the opened id) and REFUSE the save when the document's current version differs, moving the snapshot forward on each successful save. Refusing rather than remounting is the load-bearing half — remounting on every document change discards a half-typed edit, which trades one silent loss for another. Generalization: whenever a component freezes anything at mount, ask what else its write path reads live; if that thing can be replaced wholesale by any code path in the app, the freeze is a lost-update bug waiting for the two to disagree, and `key`-based remounting is a fix only when there is nothing in the buffer worth keeping. The cheap detector is a test that mutates the live input between mount and the write — not one that mounts, writes, and asserts.
## [fail:test] harness-omits-production-wiring | 2026-08-06 | count:2
A test harness that omits part of how production wires the component under test can assert loudly and measure nothing, and it fails in the direction that looks like success. Two instances in one work unit, both in the editor's React tests. (1) `tests/ui/rename-wiring.test.tsx` mounted `Edit` UNCONTROLLED — it captured `onCommit` but never fed the result back as the `source` prop, while `App` calls `setSource(next)` on every commit. Its assertions passed anyway, because the form's own local draft supplied the right key derivation regardless of what `commitDraft` received as its base; the second save was in fact PUSHING a record where production would have REPLACED one, and nothing said so until a later guard started comparing against `source` and the divergence became a hard failure. (2) `tests/ui/editor-shell-state.test.tsx` mounted `Edit` without `TranslateContext.Provider`, so every authored name resolved through the bundle-only default and fell back to its id; two assertions about what a child READS on a label were comparing an id to an id and passing. Fix in both cases: make the harness a small `Host` component that reproduces the production wiring — the controlled source round trip, and the provider built from `source.strings` — with every assertion unchanged. Generalization: when a component is CONTROLLED, or reads anything from context, a harness that skips that plumbing is testing a component that does not exist. Two smells worth grepping for: a `render()` whose `onChange`/`onCommit` callback only records and never feeds back, and a `render()` of anything below a provider that the app's root supplies. Both are most dangerous when the assertions still pass — which is the normal case, since the fallback path is usually the one that happens to produce the expected value. Related in shape to [fail:test] test-setup-hides-the-failure-path: there the setup GRANTS the condition whose absence is the risk; here it OMITS the wiring whose presence is the point.
- [2026-08-11] The 24-card audit positive controls mutated copied board or grant state directly, so they proved helper sensitivity but skipped legalActions and apply skill execution; controls now execute synthetic cards through production wiring.
## [fail:tooling] argv-prose-shell-expanded | 2026-08-06 | count:1
Passing prose through argv into a double-quoted shell string executes whatever it contains, and the damage lands in a durable record where nobody re-reads it. Concretely: a wrapup occurrence-note describing a tooling limitation named the commands it was about — in backticks, as prose punctuation — and was passed as `--occurrence-note "...`uv run pytest`..."`. Bash ran them as command substitutions before the CLI ever saw the argument, so pytest actually executed and its full session header ("collected 0 items ... no tests ran") was spliced into the middle of the sentence, while the words the backticks had wrapped vanished. The CLI exited 0 and reported success, because from its side it received a perfectly valid string. The instruction to avoid this already existed and was explicit: the wrapup stage says to write the body to a temp file with the Write tool and never embed it in argv, and gives the reason (a single apostrophe or backtick ends or re-opens the quoted word). Two things make this worth its own entry rather than a shrug. First, the failure is SILENT and durable — a corrupted memory entry looks like a written memory entry, and the next reader inherits the corruption as fact. Second, the near-miss is unbounded: the same mechanism runs arbitrary commands, and this note happened to contain `uv run pytest` rather than something destructive. The rule is mechanical, so apply it mechanically: any multi-word human text going to a CLI goes through a file written with the Write tool, or at minimum through single quotes with no backtick, `$` or `!` inside. Single quotes saved the note written thirty seconds earlier in the same wrapup; double quotes destroyed the next one.
## [fail:hook] state-set-then-read-in-one-handler | 2026-08-06 | count:1
A React state setter called earlier in the same event handler does not change what the rest of that handler reads — every function in it closes over the CURRENT render's values — so "clear the flag, then call the thing that checks the flag" silently does nothing. Concretely: deleting the record a form was holding had to close that form without asking "discard unsaved work?", because the buffer's subject was going away with it. The first attempt cleared the shell's dirty flag and then called the normal open path: `onDirtyChange(false); onOpen(kind, null)`. `onOpen` reads `libraryDirty` from the closure of the render that is still on screen, which is still `true`, so the confirm fired anyway — and the unit test that asserted "exactly one prompt" caught it only because it counted prompts rather than checking the end state. The fix is not ordering or a ref: it is to make the intent explicit at the callee, `openInLibrary(kind, id, force)`, where `force` means "there is nothing to ask about" rather than "skip the safety check", with one caller and a comment naming why the flag-clearing version cannot work. Generalization: whenever a handler wants to disable a guard it is about to trigger, passing the exemption as an ARGUMENT is the only version that works synchronously; setting the state the guard reads is a message to the next render, not to the next line. The tell is a handler containing both `setX(...)` and a call that branches on `x`.
## [fail:tooling] wrapup-land-commits-only-the-index | 2026-08-06 | count:1
`hm wrapup_land` commits the INDEX, and its `--required` / `--optional` manifest stages only documents (PLAN, memory, REVIEW, SPEC) — never source. So anything changed after `/hm:execute`'s finalize is left out of the wrapup commit unless it was staged first, and the commit that results is a half-state nobody looked at. Concretely: `/hm:execute` finalize stages the tree as it stood at that moment; `/hm:review`'s auto-fix and any user-directed fix rounds then modify source and add test files, all UNSTAGED. `wrapup_land` reported `commit: created`, its receipt listed every manifest path correctly, and the resulting commit contained the execute-era version of six source files and none of the two new test files — a tree that would not have passed the verify the wrapup had just run and reported GREEN. Nothing in the receipt says this; `steps.index_before` shows foreign staged content but says nothing about unstaged work. Fix: `git add -A` in the task worktree as part of wrapup's Step 1 pre-flight, which is where the stage already tells you to look ("if there are unstaged changes that don't trace to execute's worktree merge, surface them") — the changes DO trace to a legitimate stage, so the answer is to stage them, not to flag them. Generalization: any tool that commits an index rather than a working tree needs the caller to decide what "the work" is; a receipt that only describes what the tool itself staged cannot tell you what it left behind. The cheap check is to compare the commit's own file list against the diff you verified, before the land — `git show --stat HEAD` against `git diff --stat <base>` — because after the squash-land the branch is gone.
## [fail:test] measured-the-artifact-not-the-rendering | 2026-08-06 | count:2
A perception gate can carry a correct magnitude, a cited threshold and a proof that it can fail, and still be worthless because it measures the ARTIFACT instead of the RENDERING. A contrast harness was built to gate generated art on luminance separation, with both thresholds traced to the token file and a fixture proving it rejects a hue-only pair. Its first run against the first real asset produced three failures in a row, and every one was the harness being wrong rather than the art. (1) It measured the mark against the plain checker tones, but a square-type mark is only ever drawn on a PAINTED square — rejecting art for failing on a background it never touches. Strict in the wrong direction is still wrong; it trains people to add exceptions instead of contrast. (2) It took the MEAN luminance of opaque pixels, which cannot see an outline — and this board's entire legibility strategy, written in its own token file, is that the glyph's EDGE carries contrast while the fill carries hue. A cream mark with a near-black ring averages to "cream" and scores as invisible on a light background it reads perfectly well against. (3) It drew the asset at its native 192px, so it credited detail that the browser's downscale to a ~28px square destroys, and it discarded every pixel below 50% alpha while measuring the rest UNCOMPOSITED — so a half-opacity dark edge scored as near-black when what it renders as is halfway to the square beneath, and a 49% edge vanished from the sample entirely. Fixes, in order: derive the surface set from what the asset can actually land on; take the 10th/90th percentiles and pass if EITHER end separates (the cue-sandwich rule the board already uses); draw at the rendered CSS size and composite every visible pixel over the specific background before measuring. Generalization: for any automated perceptual gate, write down the pixels a human will actually see — the size, the background, the compositing, the surface — and measure THOSE. A gate that measures the source file is measuring the author's intent, which is the one thing never in doubt. Two of the three were caught only because a cross-model reviewer read the pixel loop rather than the threshold; the thresholds were the part everyone checked and the part that was already right.
- [2026-08-09] Fired again on the 12x12->24x24 grid raise: hand-refined sprites cleared tests/ui/art-contrast.test.ts (which reads the character table) while nine board squares failed e2e/art-rendered-contrast.spec.ts, because a 24-grid cell is 1.42 device pixels and a one-cell outline no longer occupies the rendered probe 10th percentile. The character gate cannot fail on a geometry change, so only the rasterising gate saw it.
## [fail:design] condition-narrower-than-its-case | 2026-08-06 | count:2
A feature can be fully implemented, correct, and tested, and still never run, because the condition that activates it is narrower than the case it was written for — and nothing reports that, because the code is not dead, it is merely unreached. Concretely: on a 1920x1080 window the app threw away 1440px of width (75%) and still scrolled vertically, rendering as a 480px phone column stranded in the middle of an empty desktop. The obvious diagnosis is that no desktop layout had been built. The actual one is that a complete two-column desktop layout — board in one column, sticky, everything else scrolling beside it — had existed for phases, sitting directly above the rule that was hurting, and its query was `(orientation: landscape) and (max-height: 560px)`. That is a phone held sideways. A desktop is landscape and 1080px tall, so it matched nothing and fell through to the phone base. The fix was not to write a layout; it was to open the existing one at `(min-width: 900px) and (min-height: 600px)`, which took three declarations. What hid it for so long: the project's stated target is portrait mobile web, so "desktop looks wrong" read as a known non-goal rather than as a bug, and the e2e suite runs one project (Pixel 7) with per-describe overrides at 360/768/915 — every viewport a phone or a tablet, so no test could see a viewport where the cap was wrong. It surfaced only when a human opened the app in a normal browser window. Generalization: when something looks unimplemented, grep for it before building it — a media query, a feature flag, a capability check or an environment guard can make a finished implementation invisible, and the search is cheaper than the rewrite. The tell is a stated non-goal ("we only target X") sitting next to a symptom nobody has measured: the non-goal explains away the symptom and stops the grep from happening. And when the condition is finally widened, check what ELSE the new bound now matches — a 915x412 landscape phone satisfies `min-width: 900px`, so without a height floor the desktop block would have quietly taken over a viewport an existing spec was pinning.
- [2026-08-07] Same shape, opposite direction, in the fix for the original: the new desktop band got `min-height: 700px` copied from the frame band, which draws an 844px device and needs it. A maximized browser on a 1366x768 laptop has ~630px of viewport height, and the deleted `min-width: 900px` block had NO height term — so the fix handed a common budget laptop a 390px phone column, worse than before. Floor is 600px; 1366x640 and 915x412 now pin both edges.
## [fail:lint] colour-literal-inside-a-css-function | 2026-08-06 | count:1
A colour written inside a CSS function is still a colour in a component stylesheet, and it is easy to write one while believing you are only adjusting an existing token. ADR-021 puts every colour in `tokens.css` so a re-skin and dark mode stay data changes, and `tests/ui/tokens.test.ts` enforces it by scanning component stylesheets for literals. Building the button hierarchy I reached for `color-mix(in srgb, var(--color-selected) 82%, #000)` to derive the shelf under a raised button, plus `#fff` for its lit top edge — six literals in four rules. Every one of them LOOKS like token work: the subject of the expression is a token, the literal is only the thing being mixed toward, and the intent was "the same colour, darker". The guard caught all six on the first run. The fix needed no new tokens at all — `--color-cue-hi` and `--color-cue-lo` already exist for exactly this and are deliberately theme-invariant, which is stronger than `#fff`/`#000` because the mix then means the same thing in both themes. Generalization: the rule is about the FILE, not about the declaration — any hex, `rgb()`, or named colour anywhere in a component stylesheet is a violation, including inside `color-mix`, `linear-gradient`, `drop-shadow`, and a comment's example. Before mixing toward black or white, check whether a theme-invariant token already exists; on this project one does, and reaching for a literal is a sign of not having looked.
## [fail:render] rewrite-dropped-a-documented-guard | 2026-08-07 | count:1
A guard rule whose NECESSITY is documented somewhere other than the file it lives in does not survive a wholesale rewrite of that file, because the rewrite is authored from the new design and never reads the old file's line-by-line justification. Concretely: `styles.css` carried `.editor [hidden] { display: none !important }`, and `[wiki:architecture] editor-rooms-shell-and-form-guards` records exactly why — `Edit.tsx` keeps BOTH editor panels mounted and marks the inactive one `hidden`, which is what lets a half-built room survive a trip to the library and what lets the ADR-006 vocabulary gate drive `editor-kind` / `editor-save` by test id while the child never meets either. Rewriting the stylesheet for a re-skin dropped that one line, and the new sheet then wrote `.editor > div { display: flex }` — which outranks the UA's `[hidden] { display: none }` on specificity. Nothing errored. The hidden library panel rendered underneath the rooms panel, the editor's scroll height went from ~800px to 5,440px, the shell grew to fit it, and the visible symptom was that the 만들기 screen could not be scrolled at all. The user reported the scroll, not the double-render; the double-render was two layers under it. The whole unit suite stayed green because no test asks whether a `hidden` panel is actually hidden — the ADR-006 gate drives controls in that panel BY TEST ID, which works whether or not it is visible, so the one guard most likely to notice was structurally blind to it. Fix was `.editor > div:not([hidden])`, plus a CSS-text test asserting that no rule setting `display` on the editor-panel selector may omit the `:not([hidden])` guard, plus an e2e that reads `getComputedStyle(...).display` on the hidden panel. Generalization: before rewriting a file wholesale, grep the memory tiers and the codebase's own comments for that FILENAME — a rule that exists only because of a constraint recorded elsewhere reads as arbitrary in the file and is the first thing a clean rewrite drops. The tells are `!important` and any selector shaped like a defensive override (`[hidden]`, `[disabled]`, `:not(...)`) — those are not style, they are load-bearing assertions, and each one deserves a comment IN THE FILE naming what it defends against, so the next rewrite has the reason in front of it instead of in a wiki it will not open. Related but distinct: `[fail:design] comment-claims-unbuilt-safeguard` is a safeguard described and never built; this is one built, documented, and then silently removed.
## [fail:render] unbounded-parent-defeats-inner-scroll | 2026-08-07 | count:1
`overflow-y: auto` on a box whose ancestor has no DEFINITE height is inert: the box grows to fit its content instead of scrolling, and nothing about the declaration looks wrong. Concretely: the app shell was written `main { min-height: 100dvh; overflow: hidden }` with the phone body inside it as `flex: 1 1 auto; min-height: 0`. A flex item only SHRINKS against a parent with a resolved height, and `min-height` is a floor, not a height — so with 4,700px of editor forms inside, the shell grew to 4,700px, its `overflow: hidden` clipped nothing (there was no overflow), and the editor's own `overflow-y: auto` had an unbounded parent to grow inside rather than a viewport to scroll within. Every ingredient of a scrolling layout was present and correct in isolation. The match screen hid the bug for the whole session because everything in it is `flex: none` plus a height-capped board, so it never exceeded the viewport and never needed the shell to be bounded; the defect surfaced only on the one screen tall enough to ask. The fix is one word: `height: 100dvh` alongside the `min-height`. Two lessons. First, when an inner scroller does not scroll, measure the ANCESTOR chain's `clientHeight` vs `scrollHeight` before touching the scroller — `document.documentElement.scrollHeight === clientHeight` while an inner box reports `scrollHeight` far larger is the signature of a container that grew instead of clipping. Second, `min-height: 100dvh` and `height: 100dvh` are not interchangeable on a flex container that is supposed to BOUND its children, and the difference is invisible until a child is taller than the viewport. Compare `[fail:render] sticky-inert-under-overflow-ancestor`, which is the same family from the other side — there an ancestor's `overflow-x: hidden` made a container into an unwanted scrollport; here an ancestor's missing height stopped one from being a scrollport at all. Both share the tell: the mechanism is declared on the right element and silently does nothing because an ancestor's box is not the shape the mechanism assumes.
## [fail:test] suite-attached-to-a-foreign-server | 2026-08-07 | count:6
A browser suite can report green about a checkout that is not the one you are editing, and nothing in its output says so. Playwright's `webServer.reuseExistingServer` is true outside CI by design, so if anything is already listening on the configured port the suite attaches to THAT server and never starts its own — including a dev server belonging to a different session, serving a different directory, started hours earlier. Concretely: work was being done in a per-task worktree at `.worktrees/<slug>/`, another Claude session had `vite --port 5173` running against the BASE repo, and every `npx playwright test` from the worktree tested the base repo instead. Three rounds of fixes were "verified" by 78 passing browser tests that had never loaded a line of them; the failures only appeared once the fixes were squash-landed into base and the same server finally served them. Two real regressions had been sitting there the whole time. The tell is the absence of a tell — there is no warning, the run is fast and green, and `reuseExistingServer` is doing exactly what it advertises. Three defences, cheapest first. **Check the port before trusting a green browser run**: `lsof -ti:<port>` plus `ps -o args -p <pid>` takes two seconds and names the directory the server is serving. **Run on a private port** whenever the checkout is not the canonical one — this config already exposed `E2E_PORT` for precisely the parallel-worktree case, and using it is the difference between testing your code and testing someone else's. **Do not let a fixture pin the origin**: the onboarding `storageState` here was a committed JSON file hard-coding `http://127.0.0.1:5173`, and since `storageState` is keyed BY ORIGIN, moving the port made it apply to nothing — every spec then sat on the onboarding screen timing out one tap away from the control it wanted, with nothing in the failure pointing at the port. Built from the config's own `ORIGIN` constant it follows the server wherever it goes. The generalization beyond Playwright: any test harness that can ATTACH to a pre-existing external process rather than starting one has this hazard, and the question to ask of a green run is not "did it pass" but "what did it load". A suite that cannot tell you which build it exercised has not told you anything.
- [2026-08-07] Five routing e2e failed against code I had not written: another checkout held 5173 and `reuseExistingServer` silently reused it. The config comment predicts this exact case and exists to explain E2E_PORT — which only helps someone who reads it BEFORE debugging. Cost a full diagnosis cycle; every worktree run now passes E2E_PORT.
- [2026-08-07] 2026-08-08: third instance, and the memory entry describing it already existed. A new single-player e2e spec reported all six of its tests failing on 'mode-ai not found' — including the one hot-seat control test that touches none of the new code, which was the clue: the app under test had none of my changes. Another session held 5173 and reuseExistingServer attached to it. E2E_PORT=5199 turned six failures into six passes with no code change. The lesson the prior entries already state and I still had to relearn: when a browser suite fails on something structurally impossible (a control that cannot be missing, a test unrelated to the change), suspect the SERVER before the code.
- [2026-08-08] Fourth instance: a concurrent Claude session held vite on 5173 serving master, and playwright.config sets reuseExistingServer:!CI, so every e2e run in this session silently tested ANOTHER worktree app and failed at tab-edit in a way that read as environmental — reported twice as "e2e unrunnable here" before a background-task log revealed the foreign server.
- [2026-08-08] 2026-08-08: fifth instance, and the first where a CONCURRENT SESSION was the foreign server. A wrapup re-verified after rebasing onto a base that had moved, and 24 e2e failed -- including base specs the branch never touched. The tell was an attribute: the probe read data-state=null on a hint bar whose merged code always renders it, while data-testid=rejection was present, so the served app was the PREVIOUS version of that file. reuseExistingServer: !CI had bound the run to a vite on 5173 whose /proc/PID/cwd was the base repo, started by another sessions worktree. E2E_PORT=5291 turned the same tree green: 365 passed, 0 failed. The generalization the earlier instances did not cover: on a machine running more than one worktree of the same repo, port reuse is the DEFAULT and the failure looks like a merge regression, not like a harness fault. Any e2e whose result will gate a land should either pass an explicit port or assert it started its own server.
- [2026-08-08] playwright.config runs `npm run dev` with reuseExistingServer, and port 5173 already had a Vite server from the MAIN checkout — so a worktree e2e run reported 20 failures against masters UI. The tell was a probe finding `form-panel-expert` live when grep said the source no longer had it, plus the failure split (only tests touching changed code failed). Re-run with E2E_PORT=5199. This produces a false PASS just as easily as a false failure.
## [fail:design] mode-with-no-way-out | 2026-08-07 | count:1
A control that puts the UI into a mode must offer a way back out of that mode, and "complete the thing" is not a way out when the thing may be impossible. Concretely: tapping a skill card armed it and highlighted its legal targets. `clickCard` gated only on whose turn it was, so a card the player had ALREADY SPENT armed itself — spent cards stay on screen deliberately, because AC-017 wants both hands visible with spent ones marked, and the code never distinguished visible from playable. Once armed, every tap on a non-matching square went through the pending branch, found no match, reset the target list and left the card armed; the only exit was a legal target, and a spent card has none. The player was stuck in a mode with no cancel and no legal completion, and the only recovery was starting a new match. A second, subtler instance of the same trap was found while testing the fix: `skill.recall` wants an empty square on its own back rank, which at the opening is full, so a perfectly UNSPENT card also armed into a dead end. Two independent fixes, and both are needed. **Refuse before arming**, asking `legalActions` rather than the `used` list — "this card has no legal play right now" covers the spent case and the nothing-fits case with one question, and it is the question the engine can already answer. **And make the arming control disarm**: tapping the armed card again cancels, which is how every hotbar in every game this audience plays behaves, and it is the escape hatch for any future card that can reach a state neither of the two checks predicted. Generalization: for every mode the UI can enter, ask two questions — what puts me here, and what gets me out if the thing I came to do turns out to be impossible? A mode whose only documented exit is success is a trap the moment success is unreachable, and the reachability is usually a property of game state rather than of the control. The cheap structural rule: whatever arms a mode should also disarm it, unconditionally, before any of the specific guards are written.
## [fail:test] metric-green-because-of-the-defect | 2026-08-08 | count:1
A threshold that a defect was helping to satisfy goes red when the defect is fixed, and the fix is what gets blamed. AC-012 caps the median self-play match at 40 plies over 1000 seeds, and it had been passing. One of the four card defects found by the liveness survey was rule.king-of-the-hill declaring a win whenever ANY of the movers pieces reached the centre rather than its king -- a false win condition that was ending matches early and pulling the median down. Removing it moved the median to 41 and failed the AC, on a commit whose entire content was bug fixes. The instinct to revert would have traded a correct engine for a green number. What actually resolved it was finishing the round: rule.blood-toll had been completely inert, and a card that does nothing cannot end a match either, so repairing it brought the median back to exactly 40. Both directions matter and they are the lesson. A defect that ENDS work early flatters a duration metric; a defect that does NOTHING inflates one. Rules: before treating a threshold regression as caused by the change under test, ask whether the old number depended on the behaviour just removed, and re-baseline rather than revert. And record the margin -- this AC now passes at exactly its limit with zero headroom, which is a different state from passing comfortably and should be said out loud, because the next content change reads a green suite either way.
## [fail:design] bug-masks-content-built-on-it | 2026-08-08 | count:1
Fixing an interpreter defect makes every piece of content authored while it was broken newly observable, and the content that was WRONG all along now fails in the fixers name. Concretely: the engine dropped the forEach quantifier on lifecycle triggers, so a rule cards condition was evaluated against the piece that moved rather than the piece the quantifier bound. The card rule.fast-promotion had been authored against that behaviour with a flat on_square list holding BOTH sides near-promotion ranks -- rank 5 for white and rank 2 for black in one set -- which is only survivable while the condition is asking about the mover. The moment the quantifier worked, white's entire opening pawn line, which stands on rank 2, satisfied the condition and six pawns promoted to queens on move one. The engine fix was correct; the content had been wrong since it was written and nothing could see it. Two rules follow. When a fix changes WHICH object a predicate is evaluated against, re-read every piece of content that uses that predicate, because each one was authored against the old object and the author had no way to know. And a symmetric-looking constant is the tell -- a set that lists both sides ranks, both colours squares, or both directions offsets is relying on something downstream to pick the right half, and if nothing does, it matches for everyone. The repair here was a vocabulary addition rather than a better list: no condition in the language knew absolute sides, so on_own_rank was added, counting from the subjects own home rank, and the flat list was deleted. Guard: a probe pair asserting the SAME board rank is live for one side and inert for the other, which is the only shape that can fail when a side-relative predicate silently degrades to an absolute one.
## [fail:test] gate-asserted-a-superseded-artifact | 2026-08-07 | count:1
A build-output gate can assert one artifact while a different artifact is the one that ships, and both the gate and the deploy look correct in isolation. `npm run verify` ran `build → test → test:build → e2e → e2e:pwa`, and `playwright.pwa.config.ts` serves a real build by running `npm run build && npm run preview` — a SECOND build. So the `dist/` on disk when `verify` finished was build #2, while the build-output assertions (cache-policy headers, service-worker precache completeness) had run against build #1. CI then uploaded that `dist/` and deployed it without rebuilding, under a comment claiming "the bytes that get published are the exact bytes the suite just passed against". Two Vite builds from identical source are near-certainly byte-identical because filenames carry content hashes — which is exactly why this survives: it is correct in practice and wrong in construction, so nothing ever fails and the guarantee is an assumption nobody wrote down. Fix: move `test:build` LAST in the verify chain, so the last thing to write `dist/` is the step before it reads. Generalization: whenever a pipeline both PRODUCES an artifact and ASSERTS over it, the ordering question is not "did the assertion run" but "was this the last write". Any later step that regenerates the artifact — a preview server, a docs build, a packaging step, a second test runner with its own fixture setup — silently decouples the gate from the deliverable, and the decoupling is invisible until the two builds differ for some other reason (a timestamp, a non-deterministic bundler, a plugin reading the clock). The tell is a config file that rebuilds as part of its own `webServer`/`setup` command.
## [fail:design] acted-on-an-unverified-mechanism | 2026-08-07 | count:1
A review finding can name the wrong mechanism for a real-looking symptom, and applying its suggested fix produces code that changes nothing while reading as a correction. A reviewer reported that `.play-cover > * { grid-column: 2 }` was catching `.rule-banner` and `.turn-toast` and squeezing them into the desktop side rail — plausible, specific, with the right file and a coherent trace. Two fixes were applied on that premise (span every column; then target the board's column) and moved the banner by 4px and 0px. The premise was false: both elements are children of `.play` itself, not of `.play-cover`, so the rule never reached them. What settled it was REMOVING the fix entirely and measuring — 720px with the override and 720px without it, which is what a rule applying to nothing looks like. Cost: two edit-verify cycles plus a third to undo. Generalization: a finding has two parts, the symptom and the mechanism, and only the symptom is usually verified before acting. Before applying a fix, reproduce the mechanism — not the symptom — by the cheapest available means: delete the suspect rule and confirm the behaviour changes, or read the computed value, or print the actual parent. When a fix produces a smaller effect than predicted, that is not "nearly right, adjust the numbers"; it is evidence the mechanism is wrong, and continuing to adjust is how two cycles become five. This is the reviewer-input analogue of not trusting your own reading of the cascade: the report is a hypothesis, and a hypothesis that costs one command to falsify should be falsified first.
## [fail:render] override-loses-to-specificity | 2026-08-07 | count:1
A CSS override placed in a later-loading file does nothing when a rule in the earlier file is MORE SPECIFIC, and the symptom is indistinguishable from never having written it. This fired three times in one task, in a repo whose own stylesheet already records the trap: `styles.css` says "`.phone > section` outranks a bare `.editor` or `.home`, [so] that one declaration silently won over every screen that had asked to scroll." (1) `.play { display: grid }` at (0,1,0) lost to `.phone > section { display: flex }` at (0,1,1) — and only `display` lost, so `grid-template-columns` applied and produced a flex container holding column definitions nothing could use, rendering as the pre-change single column. (2) `.play-body` at (0,1,0) lost to a `.play-cover > *` rule written three lines above it, putting the board's column in the side column. (3) `.palette` at (0,1,0) lost to `.palette.wrap` at (0,2,0), so the editor's art picker silently kept phone-sized cells. Load order is what people reason about because it is what they control when creating the file; specificity is what the cascade resolves first, and no build step, linter or type checker reports a rule that matched nothing. Generalization: when a new stylesheet layer overrides an existing one, the unit of review is not "is my rule correct" but "what is the most specific EXISTING selector for this element" — and the cheap check is not reading, it is `getComputedStyle` in the browser on the property you meant to change. Two of the three above were found that way after reading had already produced a wrong diagnosis. The mirror trap is a selector that reaches FURTHER than intended: the same task's `.dex > * { max-width: 900px }` also caught the sheet scrim (a fixed overlay, constrained to 900px on a 1440px window) and the screen header (capped, so its background and border shrank into a floating bar), both of which read as layout bugs with no obvious author.
## [fail:test] control-arm-is-not-a-control | 2026-08-07 | count:1
A measurement is only a measurement if its control differs from its arm in exactly one thing, and "exactly one" is far harder to hold than it looks — a rig that fails it reports numbers that are precise, reproducible, and about the wrong question. The grade harness in `src/balance/measure.ts` got this wrong three times in one work unit and every version passed its suite. (1) The first compared a candidate arm that appended the measured skill card to the shared draft pool against a control that left the pool alone — but every bundled card is already IN that pool and `skillPoolFor` deduplicates, so the two arms were byte-identical and all fourteen cards graded exactly 0.00 ± 0.00. (2) The fix withheld the card from the control's pool, which made the pools differ in LENGTH by one; `pickDistinct` selects with `Math.floor(rng() * pool.length)`, so the index arithmetic diverged from the very first draw and white's whole three-card opening hand differed between arms. The delta was then measuring "a different deal", the paired difference stopped cancelling, and `everChanged` read true for every card whether or not it did anything — destroying the inert-versus-gentle discrimination that was the only reason `everChanged` existed. (3) Separately, the control arm was the untouched preset, so a room that already carried a loadout was measured as "candidate INSTEAD OF the existing loadout" rather than "candidate versus the baseline". The final shape: both arms carry an explicit loadout for the measured side with the other side's stripped, and the shared pool withholds BOTH the reference record and the record under measurement while each arm appends exactly one of them — equal length, equal composition, one difference. The generalizable rule: a measurement rig needs a fixture that is IDENTICAL by construction and must therefore read as zero with zero error. Measuring the reference record against itself is that fixture, and `0.00 ± 0.00` with `everChanged === false` is unreachable unless the arms really are the same. Confirmation that the third version was right came from the numbers rather than the compiler — the skill-card standard error fell from ~4.2pp to ~1.0pp, the ~4x drop that removing a confound predicts. Related in shape to [fail:test] all-positive-fixture-hides-overcounting: in every version the suite was green because it asserted the properties the broken rig happened to have.
## [fail:test] in-sample-fit-read-as-evidence | 2026-08-07 | count:1
In-sample agreement is not evidence that a model predicts, and a model fitted on a handful of points will agree with all of them whether or not it has learned anything. The grade predictor in `src/balance/predict.ts` is the case: its first version used one feature (`33N + 0.69N²` over `attack ?? movement`) and put the strongest piece in the game near the bottom, because for a piece with a separate attack that expression returns the ATTACK alone — the archer's four jump vectors, N=2.67 — and discards that the same piece also steps in all eight directions. Splitting the axes into mobility, reach and strikes-without-occupying fixed the ordering completely: the archer went from bottom to a predicted 20.1 against a measured 20.1, every shipped piece agreed, and it was tempting to call that done. Leave-one-out said otherwise — 0.40 agreement against a 0.40 baseline, where the baseline is the trivial predictor that always answers zero. Measuring ten more synthetic pieces spanning the feature space did not move it: still 0.40 at fifteen samples, and nearest-neighbour scored 0.33, worse than guessing. The data is genuinely non-linear in a way the features cannot express: a piece that moves one square and attacks along a whole ray measures +17.4, while one that moves one square and attacks two squares away measures -11.3, and both are "ranged". The generalizable rule is that any fitted estimate shown to a user needs a score computed on data the fit did not see, and a floor to clear that is the trivial predictor for that domain — not a threshold somebody picked, but the honest question "is this better than saying nothing?". Where it does not clear, the correct product decision is to show the wait rather than the number: a provisional value no better than a guess is exactly the thing a measured value exists to replace. Built as `calibrate()`, which returns `usable` from a leave-one-out comparison against the trivial predictor, with the display path proven by a test that injects a fit which HAS earned it so the feature is dormant rather than dead. Related in shape to [fail:test] assertion-equals-its-own-default: both are a check that passes because it was scored against something it could not fail.
## [fail:test] binary-outcome-wastes-the-trial | 2026-08-08 | count:3
2026-08-08: a validator asked each expensive trial a one-bit question and then paid for the precision it discarded. The grade-price audit ran seeded matches and scored each as win/draw/loss, so separating the queen from the pawn took 40 seeds to reach barely two standard errors — 160 matches at ~0.4s, which fits a 120s timeout alone and blows it when three other files compete for cores. It timed out in two of four full-suite runs. The fix was not a faster agent or a longer timeout: the final piece difference is CONTINUOUS and every match had already computed it, giving 5.5 standard errors at 20 seeds where the binary outcome gave 2.0 at 40. Generalization: when a trial is expensive and its result is collapsed to a boolean, look for the continuous quantity the trial already produced — margin, count, distance, time — because the boolean throws away most of the information each trial cost you, and the usual symptom is a seed count tuned upward until the assertion stops flickering. A second lesson, cheaper to learn: I attributed the suite's instability to an older test in an area I had not touched, on the strength of ONE confirmed failure plus one whose detail I never captured. The confirmed count was one, not two, and the newly-added test of my own was what failed both times I actually looked. Capture the failure output before naming a culprit.
- [2026-08-17] Full Vitest under the default 20s per-test limit timed out the expensive grading-budget simulation once in two runs while competing with the suite; the same two files passed 9/9 in isolation and all 1763 tests passed with a 60s test limit. Capture the failure before attributing it to the graphics change.
- [2026-08-23] During wrapup, the same grading-budget simulation exceeded the default 20s Vitest limit in two consecutive full-suite runs under the default worker pool; the isolated test passed, and the full unit suite passed with --maxWorkers=1.
## [fail:test] jsdom-visibility-blind-spot | 2026-08-08 | count:1
Testing-library's `fireEvent` does not enforce visibility and Playwright does, so a control moved behind a `hidden` panel keeps every jsdom test green while being unreachable to a real user. This unit put the 45-control vocabulary palette into an expert TAB and, because both panels stay mounted (the `[hidden]` arrangement `editor-draft-json` already relies on), the ADR-006 coverage gate's 71 tests passed UNCHANGED — they reach controls by test id, which works whether or not the element is visible. Four separate regressions rode through on that: `editor-uses` and `editor-paired` stranded in the expert tab, boards and rooms left with an empty "simple" tab hiding the only form they have, every palette-driving e2e spec silently needing a tab click, and `editor-id` invisible because switching `editor-kind` mid-test reopens the maker gallery. All four were invisible until Playwright ran; the unit suite was 771/771 green throughout. The rule: whenever a change moves a control into or out of a hidden container, the jsdom suite CANNOT be the gate — assert visibility explicitly (`getComputedStyle(...).display`, `hasAttribute('hidden')`, or Playwright actionability), or the coverage gate you trust most is structurally blind to exactly the axis you changed. Related but distinct: `[fail:render] rewrite-dropped-a-documented-guard` is a hidden-panel guard silently deleted; this is one working correctly while the tests that should notice cannot see it.
## [fail:test] pin-that-cannot-fail | 2026-08-08 | count:2
A regression pin is worthless until you have watched it fail, and "it went red before the fix and green after" does not establish that — the RED may have come from somewhere else entirely. Two in one change: a test asserting "no second card may be played this turn" passed against the OLD engine because a card play handed the board to an opponent who held nothing, so "no card play is offered" was true by vacuity rather than by the rule; and a fixture built to pin the negamax sign rule one level down the tree passed with the bug deliberately REINTRODUCED, because the search never reached that node inside its node budget. The first was caught by a reviewer reading the assertion; the second only by reverting the fix and re-running, which is the general method — after a fix is green, put the defect back and confirm the specific test goes red, then restore. Where a pin survives that, it is coverage; where it does not, the honest move is to delete it and name the gap, because a test that cannot fail reads as coverage to every future reader and is worse than an admitted hole. Deleting it is also what makes the gap visible in review: the second case was independently re-found by the code reviewer from the note left behind. Related in shape to [fail:test] assertion-equals-its-own-default — both pass because they were scored against something they could not fail — but the mechanism differs: there the assertion matched its own default, here the scenario never reached the code under test.
- [2026-08-08] Second instance, and the vacuous thing was a REGEX. A test scanning `styles.css` for `dvh` declarations lacking a `vh` fallback used `/\bdvh\b/`, which matches nothing in `100dvh` — there is no word boundary between `0` and `d`. It reported green on the buggy stylesheet it had just been written for. Caught only by the file's own rule: run the new test against the pre-fix source and watch it go red. Two of the three tests in that change did; this one did not, and the gap was the whole assertion. Generalization: a regex is code with no type-checker and a silent empty answer. When one gates a claim, assert it MATCHES something known-bad before trusting that it finds nothing — `\b` against a digit-adjacent letter and `.` against a newline are the two that fail this way most.

## [fail:tooling] branch-outlived-the-subsystem-it-edited | 2026-08-08 | count:1
A long-running branch can be perfectly correct and still unlandable, because correctness is judged against a base that moved. This one spent a phase bumping `MEASUREMENT_REVISION` and regenerating a 600-seed grade table while `master` replaced the entire measured-grade subsystem with a declared-cost model, DELETING `cache.ts`, `shipped-grades.ts`, `predict.ts`, `bands.ts`, `grade-client.ts` and `worker.ts`. The rebase aborted on modify/delete and no test on the branch could have predicted it: the suite was green on 717 tests against files that no longer exist upstream. The tell is available cheaply and early — `git log --oneline <base>..master` plus a check of which changed files still exist there — and the review stage is the last place it can be caught before a wrapup that cannot complete. Resolution was to revert the phase wholesale and let the rebase take the deletion, keeping the MEASUREMENT in the PLAN as prose: the table was deleted, the finding it produced (eleven of thirteen skill cards went from a negative win-rate delta to positive under the new turn economy) was not. Generalization: when a branch runs longer than a few upstream commits, re-check the base before and after review, and treat "which of my files were deleted upstream" as a first-class drift question rather than something the rebase discovers for you.

## [fail:design] navigation-marked-as-unsaved-work | 2026-08-08 | count:1
A guard that asks "discard unsaved work?" fires on NAVIGATION when the code marks a navigation choice as an edit, and the refusal it produces is silent — the next save fails somewhere else entirely. Concretely: the new maker gallery's `takePick` called `onDirtyChange(true)` when the author merely chose what to start from, so picking "start from nothing" and then clicking an existing record tripped the editor shell's discard-confirm guard. Under Playwright an unhandled dialog is auto-dismissed, so `onOpen` returned false, the blank draft stayed on screen, and the save that came later was rejected for an empty `nameKey` — eleven e2e tests failing at `save()` with `must be a dotted lowercase i18n key`, none of them pointing anywhere near the gallery. A child hits the same thing: asked whether to throw away work they never did. The rule that separates the two cases: dirty means "the author would mourn losing this", not "state changed". Choosing a starting point, switching a tab, expanding a disclosure — all change state and none of them is work. Before wiring a dirty flag, ask what the author typed; if the answer is nothing, it is navigation. The tell that this class is present at all is a guard whose REFUSAL path is silent — `onOpen` returning false with no message meant a declined prompt and a refused navigation were indistinguishable from a successful one, which is why the symptom surfaced three layers away from the cause.
## [fail:test] setup-action-silently-refused | 2026-08-08 | count:1
A test's SETUP action is silently refused by a guard the harness auto-answers, so the setup does nothing, the test fails on its own precondition, and the failure reads as a product bug. Concretely: an e2e for "inspecting does not disarm an armed card" needed a skill card that can actually be armed, and the draft is seeded at random — not every shipped card has a legal target at the opening. So the test looped up to eight times: click the card, check whether it armed, and if not click `new-match` to redraw the hand. It failed with "no armable skill card after 8 draws". The redraw never happened even once: `startNew` guards a match in progress with `window.confirm`, and Playwright AUTO-DISMISSES an unhandled dialog — so all eight restarts were no-ops, the same unarmable hand was clicked eight times, and the assertion pointed at the card rather than at the setup. It masqueraded as load flakiness for two rounds because it passed whenever the first random draw happened to be armable, which is most of the time on an idle machine. Fix: `page.once('dialog', d => d.accept())` before the click. Generalization: any harness with a DEFAULT answer to an interactive prompt — Playwright dismissing dialogs, `git` refusing an interactive rebase, a CLI reading EOF as no, an installer's `--yes` absent — turns a setup step into a silent no-op rather than an error, and the loop built on it burns its whole retry budget without doing anything. Two cheap guards: assert the postcondition of every setup ACTION (did the state actually change?), not just the precondition of the claim; and when a retry loop exhausts, report what the retries DID rather than only that they failed — an eight-attempt loop that never once mutated the world should be able to say so. Related in shape to [fail:test] test-setup-hides-the-failure-path, but the inverse direction: there the setup supplies a capability and hides the absent case; here the setup silently fails to happen at all.
## [fail:tooling] minifier-dropped-the-css-fallback | 2026-08-08 | count:1
A CSS fallback written as two declarations of the same property in one rule does not survive this build, and every layer of review says it did. The canonical idiom — `height: 100vh; height: 100dvh` — relies on the cascade taking the later declaration where the unit parses and the earlier one where it does not. esbuild's CSS minifier sees a duplicate property whose later value it can keep and deletes the earlier line, so `src/ui/styles.css` carried a fallback and `dist/assets/*.css` shipped `main{height:100dvh;...}` with nothing before it. Source review passes, a source-level regression test passes, and the fix is a no-op for precisely the browsers it was written for. It reached a user first: an installed Android PWA (a Xiaomi-browser or old-WebView engine, `dvh` needs Chromium 108 while the bundle's JS target is lower) dropped the declaration, `main` lost its height, it grew to its content, `overflow: hidden` clipped nothing, the document scrolled, and the bottom tab bar — three of the app's destinations — sat below the fold. The fix is an `@supports (height: 100dvh)` block: a minifier cannot merge across an at-rule, so the shape survives. The generalization is not about CSS. **A fallback is a claim about the artifact that ships, so it must be tested against that artifact.** Any source-level assertion about a fallback is testing the input to a transform that is free to remove it — the same hazard as a bundler tree-shaking a side-effect-only import, a `terser` dropping a `void 0` guard, or a transpiler collapsing a feature-detect. The cheap check that catches all of them is to grep the BUILD OUTPUT for the thing you added and read the bytes; the durable one is a test over `dist/`, which is what `tests/build/css-fallbacks.test.ts` now does — it strips every `@supports` block and asserts the remaining cascade still gives `main` a height, i.e. it asserts over what the fallback browser actually sees. Related to [fail:test] pin-that-cannot-fail from the other side: there the test could not fail because the scenario never reached the code; here it could not fail because it was reading a file the user never receives.
## [fail:design] band-floor-below-what-the-band-can-fit | 2026-08-08 | count:1
A media-query band that draws a FIXED-SIZE thing must have a floor at least that size, or it claims viewports it cannot serve. The frame band is `(min-width: 480px) and (min-height: 700px)` and inside it `.phone` is `height: 844px; flex: none` — so every viewport 700-843px tall got a shell taller than its `main`, which is `overflow: hidden`, and the tab bar sat past the bottom edge: clipped, and unreachable because nothing in this app scrolls the document. 144px of height, and real devices live in it — an unfolded foldable, a small tablet, an Android split-screen pane. Nothing caught it because every existing test of the band renders at 900px tall, ABOVE the hole; the band's own boundary tests are all about WIDTH, since that is where the frame/desktop argument was had. The fix is one line, `max-height: 100%`, and the general rule is the one the band's own comment already applies to width and never applied to height: when a query admits a range, render at the range's WORST point, not at the comfortable one. For a fixed-size layout that worst point is the floor of every dimension the query bounds — and a `flex: none` is exactly the declaration that converts "a bit cramped" into "clipped and gone", because it removes the shrink that would otherwise have absorbed the mistake. Sibling of [fail:design] condition-narrower-than-its-case: there a query was too narrow to ever match its case, here one is too wide for the case it draws. Both are answered by the same question — name the extreme viewport in this band and render it.
## [fail:design] stale-memory-taken-as-current | 2026-08-08 | count:1
A memory entry's specific claims are a snapshot, and this repo now has two entries that were taken as current fact and were not. The costly one drove three stages: `[fail:design] declared-but-inert-vocabulary` said four shipped cards were inert or wrong-firing, so a RESEARCH document repeated it, a SPEC was built around repairing it, and the user was asked to choose an ordering — repair-first versus parallel — for work that did not exist. Running `tests/engine/card-liveness.test.ts` at the start of `/hm:plan` returned 32/32 with `intended === current` on all 29 probes; the three root causes had been closed in code by commits made after the entry was written. The same document had already flagged a SECOND entry (`shipped-grades-and-hash-keys`, describing a file and a schema field that ADR-012 deleted) as stale, which is the part worth sitting with: the author noticed staleness where the claim was structural and checkable by `ls`, and missed it where the claim was behavioural and needed a test run. The cheap check is the one nobody does. The rule: a memory claim about BEHAVIOUR is a hypothesis until the rig that produced it is re-run — before it enters a research document, not at the plan gate that happened to catch it here. Cite the run, not the entry. A corollary the entry itself predicted: it warns in its own text that "a stale entry keeps looking confirmed", and it was still believed, so the warning being present in the text is not a defence.
## [fail:design] self-review-is-not-a-review | 2026-08-08 | count:1
When the reviewer is the author, the second opinion is not a supplement — it is the review. This unit shipped 84 sprites, 24 content records and 3 rooms; `harness.yaml` configures `code-reviewer` but a session-level instruction barred subagents, so the Claude-side review was done by the same context that wrote the code. It found NOTHING. All six findings came from the cross-model `codex` pass, three of them P1, and two would have shipped content that fires and does nothing: `skill.blink` offered a play for pieces whose destination is off the board (card consumed, nothing moved) and `square.geyser` logged its trigger while leaving the piece standing. The self-review's contribution was real but was entirely downstream — it drove each report against the engine, confirmed all six, and corrected two of codex's proposed remedies. What it could not do is doubt its own assumptions, which is exactly where those defects lived: the AC that was supposed to prevent them said "every new CARD", and the author who wrote that AC also wrote the square types and never noticed the word. Two consequences. First, if the reviewer set degrades to the author, say so loudly in the REVIEW document and treat the grade as provisional — the letter is computed from findings, so a review that finds nothing grades A. Second, the gate that catches this class is a diff-blind one: the cross-model pass got only the diff and the invariants, no narrative about what the change was for, and that is what let it read the SPEC's own criterion as too narrow.
## [fail:test] benchmark-samples-what-it-measures | 2026-08-08 | count:1
A benchmark whose sample is produced by the thing it measures has no stable baseline, and its failure names the wrong culprit. `ai-bench.test.ts` builds its 32 sampled roots with `walk(seed, plies)`, which plays real plies through `chooseAction` — so the roots depend on the legal-action list, and any engine or content change reshuffles which 32 positions get measured. When a card-offer filter landed, `minDepth` dropped from 3 to 2 and the failure read as "the search got shallower". It had not: the filter REMOVES legal actions, and at the root that failed every held card's condition was `always`, so the filter did nothing there at all — measured, not assumed, by walking that root and counting (19 actions at the root, 28 on average one ply down, so depth 3 costs ~15,000 of the 20,000-node budget). The position simply sits on the budget boundary, and `depthReached` counts only COMPLETED iterations, so a search that spends 19,000 nodes inside depth 3 reports 2. The old assertion had been passing on the luck of the draw. Two lessons. When a benchmark regresses right after an unrelated-looking change, check whether the SAMPLE moved before concluding the SUBJECT did — the cheap discriminator is to re-run the benchmark with the change stashed, which takes one command and settles it. And a floor asserted per-sample (`minDepth >= 3`) over a sample the code chooses is a flake waiting for any refactor; state it as a distribution instead (no root at depth 1, nine in ten at depth 3, typical at 4), which is what "catch a search that silently degrades" actually means.
## [fail:lint] hangul-literal-in-ui-source | 2026-08-08 | count:2
The `chrome-i18n` gate forbids a Hangul literal anywhere under `src/ui/*.tsx` — and "anywhere" includes COMMENTS, which is the half that keeps catching people. It fired four times in one work unit, and the fourth is the instructive one: a comment explaining a defect quoted the exact user-facing string the defect involved (`ui.side.white` resolves to a colour name, so the comment pasted that colour name to show what the author would see). Explaining a string is not an exemption from the rule about strings; a quoted literal is still a literal, and `grep` cannot tell the difference between an apology and a shipped label. The correct way to describe player-facing text in a UI source file is to name the KEY and what it resolves to in kind — "`ui.side.white` resolves to a fixed board-colour name" — which is shorter, survives a translation change, and passes. The other three were the same shape at lower stakes: a comment naming a tab (`자세히`), a comment naming a disclosure (`고급 설정`), and a comment naming a content id. The gate has never been wrong; each fix took under a minute; the cost was entirely in the round-trips. When writing a comment in `src/ui/`, the test is not "is this user-facing text" but "does this line contain a Hangul character at all".
- [2026-08-26] Twice in one unit. First shipped: a Korean word in an App.tsx comment made the full suite fail deterministically at 1 test while every targeted re-run was green, so it read as a flake for two runs before being traced. Second caught pre-commit by grepping src/ui/*.tsx after each edit. The gate covers comments, and npm run test alone does not typecheck, so neither vitest nor tsc is the thing that catches it.
## [fail:design] threshold-puts-the-clause-out-of-reach | 2026-08-08 | count:1
A threshold can put a declared clause out of reach, and a clause nobody can reach is indistinguishable from one that was never written — while still reading, on the tin, as one of the strongest rules in the game. `rule.sudden-death`'s entire content is one clause: the mover wins once the opponent is down to `n` pieces, and `n` was 2 against a starting twelve. Its own text promised "상대 기물이 두 개 이하로 줄어들면 그 즉시 이긴다". Measured over 600 self-play matches: a side is reduced to two pieces in 1% of them, three in 5%, four in 14%. The card won three matches out of the forty-seven it presided over, and the matches it did preside over ran to the clock more often than any other card's — so the defect was simultaneously invisible (nothing errored, every test green) and load-bearing on a metric, because a card that cannot end a match inflates the median match length. Raising `n` to 4 — the smallest value whose reachability was measured above the noise — made the clause fire and moved the self-play median DOWN from 40 to 36, which is the mirror of `[fail:test] metric-green-because-of-the-defect`: there a FALSE win condition ended matches early and flattered the number, here a DEAD one ended nothing and inflated it. What made this findable was measuring the condition's reachability directly (how low does a piece count actually get?) rather than measuring the card, and what made it credible was that the 1% base rate predicted the 3-in-47 observation. Generalizations: (1) for any content rule gated on a numeric threshold, the question "can this condition be reached in real play?" is a separate measurement from "does the effect fire when the condition holds", and only the second is usually tested; (2) a per-card outcome survey needs a control — cap rates of 31-41% against a 25% baseline on samples of 56-71 are two to three standard errors and NOT a verdict, because which rule card is dealt correlates with which other cards are in the pool, so the three other suspects this survey produced were deliberately left alone; (3) the card's own player-facing text carries the threshold, so changing one without the other ships a rule that lies about itself.
## [fail:render] column-flex-wrap-invents-a-height | 2026-08-08 | count:2
A wrapping COLUMN flex container hands its single child a definite block size, and a CSS grid with a definite block size distributes that height across its auto rows — so a rule intended for row-direction siblings silently inflated a grid by 5x. Concretely: `fieldset` carries `flex-wrap: wrap` in the base rule, `.art-picker` is a `fieldset` with `flex-direction: column`, and its only child is `.palette.wrap`. A column-direction MULTI-LINE flex container takes each flex line's main size from the container's own content-box height, so `.palette` received a definite height rather than a content-derived one, and `align-content: normal` (which resolves to stretch for grid) then spread it across seven auto rows. Measured: option buttons 49x265px holding a 26x26px sprite — 225px of blank under every picture — rows at 264.7px where their content is 54px, and a 1926px fieldset inside an 844px screen. Every ingredient was individually correct and no declaration looked wrong. The static read is what failed: reading `styles.css` found nothing that could produce 264.7px, and two rounds of hypothesis-and-fix would have landed on a coincidence. What found it was bisecting single-property overrides in the RUNNING page and watching the row height — `flex-wrap: nowrap` on the fieldset dropped it to 54px, and so did `align-content: start` and `grid-auto-rows: min-content` on the grid, which together name the mechanism rather than one of its symptoms. The fix went to the root (`nowrap` on the column-direction fieldsets) and NOT broadly: forcing it on the row-direction fieldsets was measured to grow one by 267px and reflow another, so `wrap` is load-bearing there. Generalization: when a box is a size no declaration explains, measure the ANCESTOR chain's contribution by toggling one property at a time in the live page before editing anything — and treat `flex-wrap` on a column container as a size-creating declaration, not a wrapping preference. Same family as `[fail:render] unbounded-parent-defeats-inner-scroll` and `[fail:render] sticky-inert-under-overflow-ancestor` — an ancestor's box shape defeating a child's layout assumption — but the mirror mechanism: those two are an ancestor failing to BOUND a child, this is an ancestor inventing a bound the child never wanted. Guarded by `tests/ui/maker-fieldset-wrap.test.ts` (the rule, with a comment in the sheet naming what it defends) plus geometric assertions in `e2e/maker-anchors.spec.ts` that enumerate the real fieldsets, so a fifth one added later is caught.
- [2026-08-22] 2026-08-22: the new turning-slide column fieldset inherited flex-wrap: wrap and failed the cross-browser maker geometry guard; scoped flex-wrap: nowrap plus a selector-list unit guard fixed the ancestor-invented height risk.
## [fail:design] phase-done-deliverable-never-written | 2026-08-08 | count:2
A phase was reported DONE while a deliverable its own scope named was never written, and only the drift gate — the one check that compares declared scope against the actual diff — could see it. PLAN Phase 3 (ADR-004) scoped two probes: an engine-side capture oracle and `tests/ui/capture-highlight.test.tsx`, the UI half the deep interview had explicitly chosen when asked "engine only, or engine + UI highlight?". The engine half landed, cleared move generation over ~480 sampled positions, produced a genuinely useful finding, and the phase was written up as complete with measurements attached. The UI file did not exist. Nothing else in the pipeline notices this: the test suite is green because the missing test is missing, `/hm:execute`'s Phase D runs the tests that DO exist, and the phase's own exit criterion was judged satisfied by the half that was built. The report even read as more credible than usual because the delivered half carried numbers. Fix: `/hm:review` Step 2 flags files in a phase's scope that never changed as `incomplete phase`, and it did — the test was written during review and immediately passed, so this cost one review round rather than shipping. Generalizations, in order of usefulness: (1) a phase's scope list is a CHECKLIST, and marking the phase done means walking it, not recalling it; (2) an interview answer that widened scope ("both, not just the engine") is exactly the kind of decision that gets lost between planning and building, because the widened half has no failing test demanding it; (3) a completion report is most likely to be wrong when the part that WAS built went well — a clean result on one half suppresses the question about the other. Related but distinct: `[fail:design] phase-scope-omits-wiring` is a scope that never named the wiring; here the scope named it and the work skipped it.
- [2026-08-13] Second instance. The PLAN's Phase 5 scope named `e2e/a11y.spec.ts` for "the banner's live region", the phase was written up as DONE with its exit evidence quoted, and that file was never opened. The banner did carry `role="status" aria-live="polite"`, so nothing was broken — but the verification the plan promised did not exist, and the phase's own write-up read as if it did. It surfaced only at the review stage's drift gate, which compares the PLAN's per-phase scope against the actual diff and reports in-scope files that never changed; two other unchanged in-scope files had explanations recorded and this one had none, which is exactly the signal. Worth noting why it happened, because the reason is reusable: the file was unreachable for the test, not merely forgotten. `a11y.spec.ts` reaches the board through `startMatch`, which deals from a random seed, so it cannot reliably get a card played at all — the honest move was to put the assertion beside the deterministic card fixture in another spec and correct the PLAN's file list, which is what a third scope correction in the same PLAN had already done twice. The rule: when a phase's scope names a file you did not touch, the write-up must say which of the two it is — satisfied elsewhere, or not done — before the phase is marked DONE. A silent omission is indistinguishable from a deliberate one, and the drift gate is the only thing that will ask.
## [fail:design] saved-blob-forks-shipped-content | 2026-08-09 | count:1
Newly-shipped bundled content is invisible to every install that has ever saved, because the whole content set is persisted as ONE localStorage blob and the app reads the bundle only on a first run. `src/ui/App.tsx:28` `initialSource()` returns the stored source whenever `loadStoredContent` succeeds and falls through to `bundledContentSource` only when the reason is `absent`; `STORAGE_KEY = 'strange-chess.content.v1'` (`src/editor/storage.ts:19`) holds the entire `ContentSource`, and `src/ui/Edit.tsx:71` is the sole writer. So the first editor save freezes that browser's catalogue forever: a later release can add presets, rooms, pieces and cards to the bundle and the running app never looks at it again. Reported from the field as "new maps show up on a fresh install but not on an install I already had", which is exactly the observable. The only path that currently un-freezes a stored set is a SCHEMA bump making it fail validation — and that path DISCARDS the author's work (`App.tsx:37`) rather than merging, so it is a worse outcome, not a remedy; adding a preset is not a schema change and does not reach it anyway.

This is `absent-case = feature black hole` at the data layer: the stored blob predates every record added after it, carries no `bundledVersion` and no provenance per record, so nothing can tell an author's deliberate deletion of a bundled piece from a bundled piece that simply did not exist yet. That ambiguity is the whole difficulty and it is why this is a design decision rather than a patch — three candidate policies, each with a real cost: merge-missing-ids (resurrects records the author deleted), bundle-always-wins-on-bundled-ids (overwrites records the author edited), or stamp a `bundledVersion` and migrate (correct, but existing blobs lack the field, so the migration's own absent-case must be defined first or the same trap is re-dug one layer down). Generalization: a snapshot of shipped content written into user storage is a fork, not a cache — persist the author's DELTA against a named bundle version, or accept that the catalogue is frozen at first save. Found 2026-08-09 while working on an unrelated art task; deliberately not fixed there because it touches the storage policy and the content schema. Related: [[declared-but-inert-vocabulary]], [[phase-scope-omits-wiring]].
## [fail:design] metric-rewards-the-half-it-can-see | 2026-08-09 | count:1
A metric added to close one hole rewarded the half of the property it could see and opened the opposite hole, and the second defect was worse than the first. Delegating sprite drawing at the new 24x24 resolution produced art that passed every existing gate — shape, palette, run count, contrast — while being a 12x12 drawing mechanically doubled, so it added nothing the migration had already done. The fix was a metric: `subBlockDetail`, the share of aligned 2x2 blocks that are not uniform, floor 20%. It worked (candidates went from 2.8% to 25-39%) and it counts ONE-CELL FEATURES INDISCRIMINATELY, including the outline. The next round came back with one-cell outlines everywhere: outline share fell from 41-59% to as low as 14.5%, and nine board squares failed the rasterising contrast gate that the character gate cannot see. The metric was satisfied and the artifact was worse.

The diagnosis is the transferable part. `CORNER_RADIUS` was the obvious suspect and was ruled out by MEASUREMENT rather than argument — at 0.0, 0.3 and 0.6 the worst rendered mark was identically 2.43:1 with identically nine failures, so the rounding was not it. What identified the cause was the untouched control group: `bomb` 42.6%, `portal` 36.6% and `shrine` 64.9% outline share were unchanged while every refined sprite had dropped, which points at the refinement rather than at the migration or the renderer. Generalization: **a metric introduced to force a property is a proxy, and a proxy names one half of a trade-off. Before shipping one, ask what it would reward if maximised, and add the opposing metric in the same change** — here `outlineShare >= 35%` beside `subBlockDetail >= 20%`, pulling in opposite directions on purpose (detail in the interior, mass on the silhouette). Both now live in `scripts/sprite-metrics.ts` and are enforced together by `tests/ui/art-refined-pieces.test.ts`, which also carries negative tests proving each can fail. Keep a control group of untouched artifacts when changing a population — it is what turns "something regressed" into "this change regressed it". Related: [[measured-the-artifact-not-the-rendering]], [[assertion-equals-its-own-default]].
## [fail:design] restated-value-misses-a-site | 2026-08-09 | count:1
One value written at six places produced three consecutive critical review findings, each one a different missed site, and each round I fixed the sites I was shown instead of asking why the value existed six times. The value was the migration audit's baseline commit — it must be a pinned SHA and never `HEAD`, because `HEAD` moves the moment the migration commits, after which the check degenerates to `upscale(migrated) == migrated` and reports a total failure for a correct migration. Pass 1 of the plan validator found four sites still saying `HEAD` and I fixed four. Pass 2 found a fifth — the Technical Design bullet, which is the sentence the executor actually reads when it WRITES the verifier, so the mitigation was void by construction. Pass 3 found a sixth, in the ADR that owns the lossless-migration claim. Only then did I stop patching instances.

The structural fix is the lesson: **stop restating the value.** One place is declared the sole declaration — here the PLAN frontmatter field `art_baseline_sha`, with an explicit note that every other mention must REFERENCE the field name rather than carry a ref of its own — and the invariant became mechanically checkable: `rg -n 'HEAD' <file>`, where every hit must either forbid `HEAD` or sit inside the review-history table as a quotation. That grep found the sixth site instantly and confirmed zero remained; three rounds of careful prose review had not. Generalization: **an N-site edit misses a site, and prose review is the wrong instrument for a consistency invariant.** When a fact appears more than twice in a document or codebase, the defect is the duplication, not the stale copy — collapse it to one declaration and give the invariant a grep, a type, or a test. The same shape recurred inside this task at a smaller scale: a re-authoring regex over the test files matched `'spriteErrors'`, twelve characters of the sprite palette alphabet, because the guard that made the real codemod safe (the string must be alone on its line) had not been carried over to the ad-hoc one. Related: [[fix-scoped-to-the-cited-evidence]], [[comment-claims-unbuilt-safeguard]].
## [fail:test] fixture-invalid-so-fallback-satisfies | 2026-08-09 | count:2
A fixture that does not satisfy the code's own precondition reroutes execution to an ERROR or FALLBACK branch, and fallback branches routinely return exactly the value the test is looking for — so the assertion passes and the feature under test never runs. Distinct from a wrong expected value: the expected value is right, the fixture is wrong, and the wrongness is invisible because the fallback is generous. Concretely: the regression test for the bundled-content merge built an "earlier release" by removing records from the shipped bundle by id, without pruning — so surviving rooms still named cards that were gone, the document failed `loadContentSet`, and `initialSource()` took its `failedToLoad` branch, which returns the WHOLE current bundle. Every "the newly shipped record is present afterwards" assertion was satisfied by that fallback, with the merge never invoked. It was caught only because the claim "this is the only test that would have failed before the fix" was checked instead of asserted: reverting `initialSource` to its pre-fix body made 3 of 4 tests fail and left one passing, and that one was the vacuous one. No gate could have found it — the suite was green, typecheck was green, and the test-quality reviewer had passed the file. The fix has two halves and the second is the durable one: prune by real reachability (`roomsReferencing`) so the fixture is a document a shipped release could actually be, and have the FIXTURE BUILDER ASSERT ITS OWN VALIDITY (`expect(loadContentSet(next).ok).toBe(true)`) using the same validator production runs on the read path. Note that the save call gave no protection — `saveContent` is a raw `JSON.stringify`/`setItem` and validates nothing, so `expect(saveContent(...)).toEqual({ ok: true })` reads like a guard and is not one. Generalization: whenever a test seeds state that production will VALIDATE before use, assert the seed passes that same validation inside the fixture helper — and enumerate the branches the code takes when it does not, because if any of them returns a default, a superset, or a fresh copy of the thing being asserted, the test is unfalsifiable. The tell is a test whose subject is a merge/migration/upgrade and whose code-under-test has a "start clean instead" path. Related: [[assertion-equals-its-own-default]] (there the expected value is producible by accident; here the expected value is produced by a DIFFERENT BRANCH), [[test-setup-hides-the-failure-path]], and [[fix-introduced-defect-passes-all-gates]].
- [2026-08-09] Second and third instances in one change, and the second is the sharper one. (a) The loadout-exemption test mutated its base fixture to add a `loadout` without `loadoutBudget`, which `load.ts` refuses — so the assertion ran against a document the product cannot open. The file already carried a `describe('the fixtures')` guard added in response to the FIRST instance, and it validated only the BASE builder: a guard that covers the fixture you were thinking about when you wrote it, and not the mutations, makes the file look audited while the interesting state goes unchecked. Fixed by making the guard `it.each` over every named builder in the file. (b) That widened guard immediately failed a second fixture, and the reason mattered more than the fixture: a saved document carrying a dangling reference is refused by `loadContentSet`, and `initialSource()` discards such a document wholesale — so no child can ever open that room, and the test asserting "a dangling reference renders as vanished" was asserting against a state the product cannot be in. The reachable state is a dangling reference in the open DRAFT (add a piece to a room without saving, delete it from the library — allowed, because the SAVED room does not reference it), which the test now reaches through the UI. Generalization for the family: a fixture guard must enumerate every BUILDER, not every FILE; and when a fixture will not load, ask what the product does with an unloadable document before deciding the fixture is merely wrong — if the answer is "discards it", the test was unreachable rather than invalid.
## [fail:test] rule-checked-where-it-was-announced | 2026-08-09 | count:1
A reviewer asked "is this test vacuous?" answers by checking the place the anti-vacuity rule was ANNOUNCED, not the places it was broken — and then reports the whole file clean, which is worse than not asking. Concretely: `tests/content/merge.test.ts` opens with a header stating the rule in so many words ("No fixture may have the saved set equal to the bundle for the row it tests"), applies it correctly across the seven-row fixture block it was written for, and then violates it in the LAST test of the same file, which merged the bundle into itself so that `added` was empty for any stamp at all — including a `stampOf` that returned no ids. The `code-reviewer` pass traced that seven-row block, found the rule satisfied there, and concluded in writing that "the test suite is non-vacuous — it follows the plan's own assertion-equals-its-own-default discipline". The Phase A.5 `test-reviewer` gate had already listed the same test under `passing_tests` with zero blocking issues. A heterogeneous cross-model voter reading the same diff found it, plus a second one of the same shape in another file. Two gates certified as live two tests that could not fail. The asymmetry is worth naming: a stated rule makes the file LOOK audited, so a reviewer's attention is drawn to the block the rule discusses and away from the rest — the header functioned as an alibi. What settled it was not preferring a voice but mutating: `stampOf` was made to return `{ ids: [] }` and the corrected test went red where the original had passed; the second finding was confirmed by injecting the exact regression it failed to catch. Generalization: when a file declares a testing invariant in prose, treat that declaration as a LIST OF PLACES TO CHECK, not as evidence, and check every test in the file against it — especially the ones added last, which is where a rule stated at the top has had the most chances to be forgotten. And for any claim of the form "this test would have failed before the fix", run the pre-fix code; the claim is free to make and cheap to check, and it is the only way to distinguish a regression gate from a test that merely happens to be green. Related: [[assertion-equals-its-own-default]], [[fix-scoped-to-the-cited-evidence]] (the same narrowing, applied to a fix rather than to a review), and [[fixture-invalid-so-fallback-satisfies]].
## [fail:design] fix-introduced-defect-passes-all-gates | 2026-08-09 | count:2
A fix applied after the last reviewing eye has left ships the next defect, and the gates that were green for the first one are green for the second. This entry existed only as a dangling `[[link]]` from three other failures until a review reproduced it live: round 1 found `cycleAt` silently lengthening a ray on the axis the author was NOT editing; the fix made the new ray adopt the existing cap; round 2 found that adopting drew the square the author DID tap as untouched while a square they never tapped became the tip. Same defect class — an edit the user cannot see — moved one axis over. Round 1 also rewrote a hint string that described a deleted four-state cycle, and the replacement was itself false for 24 of the 48 cells. Every one of those rounds was fully green: `tsc` clean, 1387 vitest, 131 Playwright. The tell is that the fix and its verification are authored by the same pass: a patch written to close a finding is tested by a test written from the same mental model, so it inherits the model's blind spot. Two remedies, and the second is the one that actually worked here. First, cap the fix rounds and STOP rather than patching again — a change made after the last review is unreviewed by construction, and `max_review_rounds` is exactly that cap. Second, when two consecutive fixes to one site produce two variants of the same defect, the premise is wrong rather than the patch: here one `reach` value was serving two questions (which axis, which direction), so every possible tap handler had to lie to one of them. Widening the model (`reach` per direction AND axis, which the persisted schema already permitted) removed the class instead of guarding a symptom, and round 3 was the first round in which a fix introduced nothing new. Generalization: count the fixes at one site. At two, stop writing handlers and go read the data model — and when the schema is already wider than the in-memory representation, that gap IS the bug.
- [2026-08-13] Second instance, and this one was produced by a fix applied during the review round that found the first. A reviewer flagged that the card banner's name fell back to the raw content id on a lookup miss, breaking the rule that no UI component may name a record by id. The fix resolved the record once and made the banner's visibility depend on it — and stopped there. The ring's `impacted` set, three lines below, still read the latch alone. So an unresolvable card would have drawn rings on the board with no banner anywhere to explain them, while some other notice held the slot. The comment directly above `impacted` already promised the two share a lifetime; only the code did not. Every gate was green when the fix landed: typecheck clean, 873 unit tests, 430 e2e. It was caught because the second review round re-reviewed THE FIX rather than the feature — the reviewer was handed the two diffs and asked what they newly made reachable, and traced `cardBannerUp` against `impacted` as its first move. The generalization that keeps paying: when a fix makes one consumer of a value stricter, enumerate every OTHER consumer of that same value in the same commit, because the gates were all written against the pre-fix invariant and none of them knows it moved. The triggering input here is unreachable through the component's own API, so it has no scenario test; it is pinned instead as an invariant that samples the banner and the ring together before a card, while it is announced, and after it clears, and requires them to agree at each point.
## [fail:design] verification-chosen-before-reproduction | 2026-08-11 | count:1
A verification approach chosen BEFORE the defect has been reproduced is designed against an imagined failure mode, and the plan will defend that choice with reasons that sound sound because nothing has contradicted them yet. Concretely: a button's label was reported cut off, the mechanism was correctly derived by reading CSS, and the plan then decided — through a structured interview, twice, with the alternative explicitly offered and explicitly declined both times — that a static stylesheet-text guard plus a manual visual sweep was sufficient and a geometric e2e was not needed. That decision became ADR-004 with a recorded consequences block. Both halves turned out to be built on a premise nobody had tested. The static guard proves rule SHAPES are absent and passes happily on a stylesheet whose buttons clip, which the ADR did record. What it could not record, because it was not yet known, is that the manual sweep was scoped to five viewports at which the defect DOES NOT REPRODUCE: the squeeze needs a column that overruns its shell, and at 390x844, 412x915, 768x1024, 915x412 and 1280x900 the column fits, so the shrink path is never taken and the button already renders at full height. Reproduction at 360x560 (52.2px and clipped with the fix reverted, 70.8px without) is what falsified it, and that happened during execute, after planning had closed. The fix was to add the geometric e2e anyway and record the ADR reversal — but the reversal cost a full plan-validator cycle plus a review round, and a plan whose verification section is wrong is worse than one with no verification section, because the wrongness is invisible and carries an ADR's authority. Generalization: when a task is a BUG FIX, reproduce before deciding how to verify — the reproduction condition is an input to that decision, not a detail of executing it. Two tells that the decision is being made too early: the interview offers a verification option and the argument for declining it is about cost rather than about coverage, and the sweep/test viewports or fixtures are named from the environment where the bug was REPORTED rather than from a run where it was OBSERVED. A related, cheaper guard that did work here: the added e2e asserts as its FIRST test that its viewport still overruns the shell, so if the reproduction condition ever disappears the suite says so instead of passing for the wrong reason.
## [fail:test] green-test-that-cannot-discriminate | 2026-08-14 | count:1
Three gates in one unit passed on tests that could not tell a correct implementation from a wrong one, and every instance was found by MUTATION rather than by review. (a) A settlement guard was changed from occupancy-only to occupancy-plus-identity; reverting the identity comparison left all 16 tests green, so the fix was defended by nothing — two fixtures were attempted, neither reproduced the defect, and the honest outcome was to DELETE the test and record the gap in a comment at the same location rather than keep a green assertion that proves nothing. (b) An editor numeric field's cleared-value fallback was special-cased to 0 for a `nonnegative` condition; flipping it back to the shared `?? 1` left all 96 tests green. (c) Earlier in the same unit, a discrimination reviewer showed that every AC-013 fixture painted only bomb squares, which destroy IN PLACE — so pre-cascade and post-cascade squares were always identical and an implementation threading the pre-cascade target passed identically. The remedy in each case was a fixture where the two answers DIFFER (a portal, whose destination is not its entry square; a cleared field, not a set one). Generalization, and the cheap habit that catches all three: after fixing something, revert the fix and re-run. A green suite before and after means the test is decoration. This costs one command and it caught three real gaps in a single unit — the cost of not doing it is a defended-looking change with nothing behind it.
## [fail:design] content-coordinates-are-board-size-bound | 2026-08-14 | count:1
A shipped record's coordinates are only correct for the board size they were written against, and nothing in the schema says so. Four bundled rule cards encode 6x6 geometry: `king-of-the-hill` and `harvest` name `CENTRE` = c3/c4/d3/d4, and `fast-promotion` and `beacon` read `on_own_rank` 5 and 6 — one short of promotion, and the opponent's back rank, on SIX ranks only. On the 8x8 room added in v12 all four still FIRE, just somewhere the card's own text does not describe: the "centre" squares are off-centre and "one short of promotion" is three short. That is worse than not firing, because a dead card is eventually noticed and a card firing on the wrong square reads as the engine being wrong. Found while choosing the new room's card pool, not by any test — the loader validates that a square REFERENCE is inside the board, so c3 on an 8x8 board is perfectly valid and says nothing about whether it is the centre. Handled by excluding all four from `preset.grand` and pinning the exclusion with a test, so the next 8x8 room (which will be written by copying this one) inherits the list rather than rediscovering it. The real remedy is a board-relative vocabulary — a rank counted from the far end, a "centre" the loader derives from width and height — and that is a schema change, not a content one. Generalization: any content field holding an absolute coordinate is implicitly parameterised by board size; adding a second board size turns every one of them into a silent correctness question, and the loader cannot ask it for you.
## [fail:tooling] hm-cli-not-on-path | 2026-08-16 | count:1
The harness stage command failed before doing any work because the `hm` executable was not on PATH in this Codex runtime, while the project instructions assumed a globally available command. Fix: invoke the pinned installation explicitly with `uv run --project <harness-maker-cache> hm ...`, and check the installed version when the rendered skill version drifts from the plugin version. Generalize the stage runner so its documented entrypoint matches the runtime's actual installation path.
## [fail:tooling] review-telemetry-measured-payload | 2026-08-22 | count:1
The review telemetry producer was initially invoked with a finalize payload under `--file`, which violated the telemetry input schema; the run recovered by constructing the documented telemetry record and re-emitting it. Use the producer record schema, not the finalize envelope, when emitting review telemetry.
## [fail:tooling] verification-cache-python-entrypoint | 2026-08-23 | count:1
The wrapup verification cache command was initially run as `python -m harness_maker.cli observability.verification_cache`, but the installed CLI exposes the cache through `hm observability.verification_cache`; this runtime also has no unqualified `python` command. The command failed before doing work and was corrected by using the pinned `uv run --with harness-maker hm ...` entrypoint. Keep the stage skills' executable form aligned with the runtime and verify subcommand help before retrying.
## [fail:test] webp-baseline-float-cross-engine | 2026-08-23 | count:1
The S1d unchanged-art regression gate failed in mobile-webkit because the fixture compared browser-decoded WebP luminance-derived floating metrics to a Chromium-generated baseline at twelve decimal places; the underlying discrete pixel counts were unchanged and the observed difference was only 3.5e-7. The fix keeps exact equality for opaque-pixel counts and uses a five-decimal tolerance for engine-sensitive floating metrics, preserving meaningful asset-change detection while allowing legitimate decoder rounding differences across browsers.
## [fail:test] mobile-webkit-parallel-e2e-flake | 2026-08-23 | count:1
The full Playwright run intermittently failed two existing mobile-webkit tests under the seven-worker matrix: a layout boundary evaluated before the .phone element existed, and a match lifecycle move exceeded its 90-second timeout. Both passed on immediate targeted reruns, while the changed S1d/S1e tests passed in all browser projects. The likely root cause is browser-worker contention and WebKit timing rather than this asset change; retain the coverage and isolate/retry the named failures before attributing a product regression.
## [fail:tooling] scripted-edit-matched-nothing | 2026-08-26 | count:1
A scripted edit that silently matches nothing leaves the code unchanged, and the stage that ran it reports the work done — the edit's own success is never checked, so the gap is invisible until someone reads the file. Concretely: `startNew` in `src/ui/MatchHost.tsx` needed `setDiscovered(null)` added alongside its dozen other per-match resets, and the anchor used for the replacement was `const s = newSeed()\n    setPlay({...})`. The real file has `setLastMove(null)` between those two lines, so the Python `str.replace` matched zero times, wrote the file back byte-identical, and the execute stage reported Phase 5 GREEN — the phase's own tests passed, because none of them exercised a rematch through one component instance. Three separate review lenses (concurrency, robustness, design) later found the missing reset independently, and confirming it took one `grep -n setDiscovered`. The failure is not the wrong anchor; wrong anchors are ordinary. It is that a no-op replacement and a successful one are indistinguishable to a script that does not assert, and that "I added X" was then reported to the user as fact. Fix, applied for every subsequent edit in that work unit: end each replacement script with `assert s != before` so a no-op raises, and grep the file for the new symbol afterwards. The same shape hides in `sed -i` (a pattern that matches nothing exits 0) and in any `Edit`-like helper whose failure mode is "did nothing" rather than "raised". Generalization: when a tool's failure mode is silence, the caller must supply the assertion — and a claim about a file that was never read back is a claim about what the tool was asked to do, not about what it did. Related in shape to [fail:design] comment-claims-unbuilt-safeguard, where the artifact describes a safeguard nobody built; here the artifact describes a change nobody made.
<!-- @hm:/user:entries -->
